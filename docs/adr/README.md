# Architecture Decision Records

This directory holds Burrow's Architecture Decision Records (ADRs): one decision per
file, numbered, each with its context, the decision, its consequences, and the
alternatives that were rejected.

## Conventions

- **One decision per file.** Files are named `NNNN-short-title.md`.
- **Accepted ADRs are immutable.** Once an ADR is Accepted, its body is not edited.
  A changed or reversed decision is a *new* ADR that names exactly what it supersedes;
  the only edit permitted to the superseded record is its Status line
  (`Superseded by ADR-00NN`). Fixing a typo or a dead link is fine; adding reasoning is
  not — new rationale belongs in a new ADR.
- **ADRs record decisions, not implementation status.** "Accepted" means the decision
  is made, not that the code exists. An ADR ahead of the code is normal. Never write
  "implemented / not yet implemented" into an ADR — on acceptance the work becomes
  **GitHub issues labelled `adr`**, one per implementable unit, and the open set under
  that label is the decided-but-unbuilt list ([ADR-0070](0070-implementation-status-lives-in-issues.md)).
  The issue names its ADR; the ADR never names the issue. [docs/ROADMAP.md](../ROADMAP.md)
  keeps the themes and the sequencing so the repository still describes itself offline.

## Format

New ADRs follow [template.md](template.md): a **Status** badge, then a one-paragraph **TL;DR**
(the decision in brief, with its relationships to other ADRs), then Context, Decision,
Consequences, and Rejected alternatives. Status badges:

✅ `Accepted` · 🟡 `Proposed` · ❌ `Rejected` · ♻️ `Superseded by ADR-00NN`

Earlier records predate this format; it applies going forward (Accepted ADRs are immutable, so
they are not reformatted).

## Using this index

Each row states the decision, names the mechanism concretely, calls out the constraint that
makes it work, and ends with the alternatives that lost — enough to answer most questions
without opening the record. Where a later ADR supersedes or refines part of an earlier one,
the row says so inline in **bold**, so a reader can tell which parts of an older record still
bind before opening it. The **Status** column reproduces each record's own status line and is
the authoritative one; the inline notes are navigation aids.

## Companion documents

Some decisions and operational contracts live outside `docs/adr/`, because they track the code
rather than freezing at a point in time. ADRs point at these files instead of restating them,
so they are listed here:

- [**docs/CAPABILITIES.md**](../CAPABILITIES.md) — the **"can Burrow do X?" reference**: what
  each capability does, the command that reaches it, the ADR that decided it, and the sharp
  limit next to it, organized by what a user wants to do rather than by package. Source of
  truth for what is actually built versus decided-but-unbuilt, so a reader need not infer
  absence from one code path (private-registry pull and the Postgres add-on both sit off the
  deploy path) or presence from an Accepted ADR ([ADR-0009](0009-honest-status.md)).
- [**docs/HARDENING.md**](../HARDENING.md) — the **agent trust-surface model and the
  scoped-credential workflow**. Source of truth for the four unequal credentials that reach a
  cluster (your admin kubeconfig; `burrow` for setup and governance; `burrow` for app
  operations; `burrow-agent`), exactly what each can and cannot do, `burrow-agent`'s
  fail-closed behaviour and `BURROW_AGENT_REQUIRE_SCOPED=1`, the multi-user join and the opt-in
  `burrow-agent-token` reader Role an operator may add for self-serve joins, and the worked
  agent permission-rule example. [ADR-0021](0021-guardrails-require-control-plane-only-agent-access.md)
  and [ADR-0038](0038-scoped-agent-credential.md) both defer to it, and it carries the honest
  limitation neither repeats: the scoped credential confines the agent *to* burrowd but does
  not by itself enforce *which* burrowd operation it may call.
- [**docs/ARCHITECTURE.md**](../ARCHITECTURE.md) — the system design narrative: the four
  layers, the load-bearing invariants, the request paths (deploy, status, logs, scale,
  rollback, publish), the components, and where state lives. The ADRs record *why* each piece
  is shaped as it is; this is *how they fit together* at runtime.
- [**README.md**](../../README.md) — its version status table is the **authoritative
  shipped / in-progress / planned record** ([ADR-0009](0009-honest-status.md)). Because ADRs
  deliberately carry no implementation status, this table is the only place to learn whether a
  decision below has shipped.
- [**docs/ROADMAP.md**](../ROADMAP.md) — the coarse v0.x → v1.0 milestones, version by version,
  and a home for decided-but-unbuilt work that an ADR may not claim as done.
- [**docs/PLAN.md**](../PLAN.md) — the current execution plan: what is being worked now and
  next. The front line only, pruned as work lands.
- [**LICENSING.md**](../../LICENSING.md) — the operative licensing contract: Apache-2.0 across
  the whole repository, the per-file `SPDX-License-Identifier` rule enforced by
  `scripts/check-spdx.sh`, and the note that keeping `controlplane/` and `operator/` out of the
  top-level `internal/` is a **module** boundary, not a license boundary. Implements
  [ADR-0033](0033-relicense-to-apache.md) and [ADR-0045](0045-oss-enterprise-boundary.md).
- [**CONTRIBUTING.md**](../../CONTRIBUTING.md) — the contribution contract that
  [ADR-0001](0001-license-and-dco.md)'s surviving half decided: issues and discussions are
  fully open, outside code is coordinated with the maintainer and CLA-gated, and every commit
  carries a DCO sign-off (`git commit -s`).
- [**template.md**](template.md) — the required shape of a new ADR: status badge, TL;DR with a
  relationships clause, context, decision, consequences, rejected alternatives.

## Index

| ADR | Title | Status |
| --- | --- | --- |
| [0001](0001-license-and-dco.md) | License, contribution policy, and DCO — the founding posture: a **split license on the package boundary** (Apache-2.0 on the client surface `cmd/burrow/`, `mcp/`, `internal/`; FSL-1.1-ALv2 on `controlplane/`, `cmd/burrowd/`, `operator/`, each release auto-converting to Apache-2.0 on its second anniversary), paired with **sole copyright ownership**, a sell-exceptions business model, and a contribution policy where community *input* is unrestricted but outside *code* is declined or CLA-gated. The load-bearing constraint is that selling license exceptions is possible only while one party holds 100% of the copyright, so merging outside code under a bare sign-off forfeits that option for that code permanently — which is why this record explicitly reverses an earlier draft's DCO-over-CLA conclusion. DCO sign-off (`git commit -s`) stays required on every commit as a provenance attestation: necessary, but for outside code not sufficient. FSL over BSL for its shorter text and its fixed two-year conversion (against BSL's common four); the FSL packages sit outside the top-level `internal/` so a separate private module can import their public API. **The license half is superseded by [ADR-0033](0033-relicense-to-apache.md)** (whole repo to Apache-2.0); the ownership, DCO, and CLA half remains in force. Rejects all-Apache (no resale protection, no basis for selling exceptions), all-BSL/FSL (non-OSI friction on the adoption-critical client surface), DCO-only community merges, and a non-converting source-available license such as SSPL | Accepted (license decision superseded by ADR-0033; DCO/CLA stance stands) |
| [0002](0002-four-layer-architecture.md) | Four-layer architecture; the control plane is the product — four layers with a sharp ours/not-ours line: (1) **the agent**, not ours, untrusted and interchangeable; (2) **the thin MCP server**, translation only, no cluster credentials and no orchestration logic — the remote control, not the engine; (3) **the control plane, the product** — deploy orchestration, the build-to-image pipeline, rollout and rollback, logs and status, scaling, the guardrails, the deploy record, and the only layer holding cluster credentials or calling the Kubernetes API; (4) **Kubernetes**, targeted but not part of Burrow. The load-bearing consequence is that the control plane's API is the real interface and every front-end (the MCP server then, the CLI, a later dashboard) is a peer client of it, so the security boundary and the single home for guardrails coincide **structurally** rather than by convention, and the control plane keeps its own state independent of cluster state. Each temptation to collapse a layer trades away one of those properties. Rejects collapsing the MCP server and control plane into one process (puts cluster credentials behind the MCP connection and couples a control surface that will change to an engine that should not), letting the MCP server call Kubernetes directly for "simple" operations (two homes for guardrails and the deploy record — and the simple operations are exactly the ones that get dangerous), and treating the agent as a trusted internal component | Accepted |
| [0003](0003-agent-neutral-mcp-control-surface.md) | An agent-neutral MCP control surface — Burrow's agent-facing surface targets the **MCP standard**, not any one vendor's integration API, and nothing in the server or the control plane branches on which agent is calling: agent-neutrality is a property of the design, not a configuration. Tool definitions, descriptions, and result shapes are written for a general tool-using agent rather than tuned to one client's prompting quirks or proprietary extensions. The constraint that follows, and that outlives the transport: because the caller is autonomous, **every tool returns a structured, machine-reasoned result** — success or failure, what changed, how to undo it — never prose meant for a human, which is the same requirement guardrails impose in [ADR-0006](0006-guardrails-in-the-control-plane.md). A richer single-agent integration, if ever justified, layers *on top of* the neutral surface and never specializes it. **Preserved, not superseded, by [ADR-0049](0049-burrow-agent-scoped-cli-control-channel.md)**: a CLI any agent can run is at least as neutral as an MCP server, so the principle survives the move off MCP verbatim. Rejects binding to a single agent's native integration API (couples Burrow to one vendor's roadmap in an unsettled market and excludes every other agent's users), inventing a Burrow-specific agent protocol (asks every agent for bespoke support — the friction MCP exists to remove), and supporting several agent protocols in parallel | Accepted |
| [0004](0004-code-never-over-mcp.md) | Code never travels over MCP; the registry is the conveyor belt — the agent's channel carries only **tool calls and small metadata** (an image reference, environment variables, a command, a replica count); the built container image moves through a **container registry**, pushed by whoever built it and pulled by the cluster's nodes. The mental model is explicit: *the control channel is the remote control; the registry is the conveyor belt*, and a deploy is pressing a button naming an item already on the belt. The reasoning is that a tool-call channel is a control path, not a data path — pushing image bytes through it would be slow, would strain message-size limits, and would throw away the content-addressing, deduplication, and caching that registries exist to provide. Two consequences are load-bearing elsewhere: `deploy` takes a pullable reference plus small metadata, and content-addressing makes rollback nothing more than naming a different digest already on the belt. **Its "over MCP" wording is generalized to any control channel by [ADR-0049](0049-burrow-agent-scoped-cli-control-channel.md), which explicitly does not supersede it** — the invariant is transport-independent and survives verbatim in substance; [ADR-0053](0053-in-cluster-build-from-source.md) honours it by sending only a git ref and cloning inside the cluster. Rejects streaming image bytes through the channel, having the control plane accept an image upload and push on the caller's behalf (a bytes-handling middleman that blurs the "reference in, run out" contract), and baking code into a ConfigMap or passing it inline | Accepted |
| [0005](0005-mcp-server-holds-no-cluster-credentials.md) | The MCP server holds no cluster credentials — the credentials that operate Kubernetes live **only in the control plane**, the one layer that talks to the cluster; the MCP server authenticates to the control plane and forwards tool calls, never possessing, proxying, or seeing them. This places the security boundary at the control plane rather than at the most-exposed, least-trusted-adjacent component: compromising or impersonating the agent-facing layer yields no cluster keys, so that layer can be deployed in more-exposed positions without widening the blast radius, and there is exactly **one credential holder to secure, audit, and rotate**. The question "may this operation touch the cluster?" is therefore answered behind the credentials by the guardrails, not at the edge where the agent connects. **Superseded by [ADR-0049](0049-burrow-agent-scoped-cli-control-channel.md)**, which migrates the identical principle to `burrow-agent` — the boundary does not move, only the name of the thin client — and **refined for self-host by [ADR-0014](0014-self-host-connectivity-via-kubeconfig.md)** (the local client may use the developer's own ambient kubeconfig purely for *connectivity* through the API-server proxy) and then by [ADR-0038](0038-scoped-agent-credential.md) (Burrow mints a scoped, burrowd-only credential, so that connectivity credential need not be cluster-admin). Rejects storing cluster credentials in the MCP server, giving the agent cluster credentials directly with Burrow as a pass-through, and splitting credentials across both layers | Accepted (superseded by ADR-0049; the credential boundary migrates to `burrow-agent`) |
| [0006](0006-guardrails-in-the-control-plane.md) | Guardrails live in the control plane — safety is enforced at the same boundary that holds the cluster credentials, so every operation that can touch the cluster passes through it no matter which client initiated it. Two rules define them: **dangerous operations are gated or refused behind the credentials** (confirmed, constrained, or refused outright per policy), and **every operation returns a structured result the agent can reason over** — success or failure, what changed, the current state, and where relevant how to undo it. The load-bearing argument is elimination: the agent is untrusted and interchangeable, and the agent-facing layer is a thin, replaceable front-end with the CLI as a parallel client, so a guardrail at the edge is bypassed the moment someone uses the other client. Because the guardrails sit at the credential boundary, **safety holds for every client by construction**; structured results become part of the API contract rather than a presentation concern; rollback is a first-class guarded operation rather than an afterthought; and the whole thing is unit-testable against faked Kubernetes, registry, and database seams. This ADR fixes *where* guardrails live and *that* results are structured; it deliberately does not enumerate the policy — [ADR-0020](0020-guardrails-as-configurable-policy.md) makes that policy inspectable and configurable and adds the `confirm` disposition. Rejects enforcing safety in the MCP server, trusting the agent through prompts and tool descriptions (which can advise but cannot gate), and returning prose results for an autonomous caller to parse | Accepted |
| [0007](0007-explicit-deploy-by-image-reference.md) | Deploy is an explicit call by image reference — the agent names an image already in a registry the cluster can pull from, plus small metadata, and the control plane performs a guarded rollout. Passive image-tag watching (the GitOps auto-deploy model) **may exist as an optional mode but is never the spine**, because the explicit call is where Burrow's value concentrates: it is the only place the guardrails run, the structured feedback is produced, and the rollback handle is created. Three properties follow. Every deploy is gated and returns what changed and how to undo it; every deploy **creates a rollback handle**, because the control plane records which digest, when, by whom, replacing what — the recorded deploy *is* the rollback handle; and deploys are deterministic and auditable, because a specific reference is named rather than resolved from a floating tag at some unobserved moment. Any passive mode added later must route through the same guarded path — exactly the constraint [ADR-0052](0052-pull-based-passive-deploy.md) honours when it adds the semver-scoped registry watcher, [ADR-0053](0053-in-cluster-build-from-source.md) honours when it makes build an explicit front-end that ends where deploy begins, and [ADR-0048](0048-one-off-command-runner.md) echoes for command execution. Rejects passive tag-watching as the primary mechanism (no natural place for the guardrails, no structured result, a fuzzier rollback story), deploy by git reference in v0.1, and deploying without recording the prior state | Accepted |
| [0008](0008-two-build-paths.md) | Two build paths for two users — the image has to reach the registry before deploy, and two distinct users have two distinct expectations, so Burrow supports both and selects by who is operating. **Client-side build** (the self-host developer, and the only path in v0.1): the agent or CLI builds locally, pushes to a registry the cluster can pull from, then calls deploy with the reference — Burrow builds nothing. **Server-side build** (later, for a platform user): the platform builds from a git reference, pushes, and deploys the result. The load-bearing property is that **both converge on the same thing — an image in a registry, deployed by reference through the same guarded path** — so the deploy contract is build-path-agnostic and the control plane never needs to know which path produced the artifact. The anticipated server-side builder runs where it lives and pushes itself; it never accepts image bytes over the control channel ([ADR-0004](0004-code-never-over-mcp.md)). **[ADR-0053](0053-in-cluster-build-from-source.md) later adds a third shape to this taxonomy** — a build in the *user's own* cluster from a git ref — which is neither the laptop nor a platform, and keeps client-build-first as the default. Rejects server-side-only (imposes build infrastructure and git triggers on a developer who already has a toolchain), client-side-only (forecloses point-at-git convenience), and one configurable path doing both (premature: the two run in different trust and compute environments and already converge where convergence is needed) | Accepted |
| [0009](0009-honest-status.md) | Honest status: docs never describe unbuilt behavior as done — everything in the docs is a goal until it ships, enforced concretely: the **README carries a version status table** (`🚧 in progress` / `✅ shipped` / `planned`) that never lags the code and flips in the same change set that cuts a release; unbuilt behaviour is written in a planned voice, never the present tense of a working feature; **ADRs record decisions, not status**, so an Accepted ADR describing unbuilt behaviour is normal and correct and status lives in the README table, the roadmap, and the plan instead; and tool descriptions and structured results describe only what a tool does today, with a stubbed operation saying so rather than implying success. The reason this is a rule rather than a style preference is the reader: **an autonomous agent is a primary consumer of Burrow's surface and will *act* on what it reads**, so dishonest status is not a marketing smell but an active hazard. The consequence is that doc review carries a status-honesty check — any present-tense claim must correspond to shipped, tested behaviour — and the discipline is mirrored as a repository convention so every contributor applies it. Rejects writing aspirationally in the present tense and fixing the docs later, and tracking implementation status inside ADRs (it conflates the decision record with the build tracker, and ADRs are immutable while status changes — the two must not share a home) | Accepted |
| [0010](0010-testing-strategy.md) | Testing strategy: seam-isolated units, ephemeral-cluster integration, targeted fault injection; **no global simulation harness** — the record exists to make a deliberate divergence reviewable rather than an unexamined omission. Burrow does not implement consensus, durability, or leader election; it **delegates** them to Kubernetes/etcd and Postgres, so the catastrophic-correctness surface that justifies a sibling project's simulated disk, network, and crash harness does not exist inside Burrow, and porting it would mean reimplementing the correctness of the very systems Burrow chose not to own, in order to test code that does not contain those bugs. Three layers instead: (1) **seam-isolated unit tests against fakes** — Kubernetes, the registry, **the clock**, and the database all behind injected interfaces, with **no ambient time or I/O in core logic** (no `time.Now()`, no direct network or filesystem access), deterministic by construction; (2) **ephemeral-cluster integration** (kind or k3d) running the real deploy, rollback, logs, and scale paths against a real API server; (3) **targeted deterministic fault injection** for the only parts with distributed-systems-shaped bugs — the deploy state machine and the operator reconcile loops — driving a fake client through adversarial event orderings and injected API errors under a seeded schedule, plus controller-runtime envtest. Determinism comes from injected dependencies and seeded schedules, not a global event simulator. If Burrow ever took on self-implemented consensus or durability, this would be revisited in a new ADR. Rejects porting the global simulation harness, integration-tests-only (too slow and coarse for adversarial orderings), and fakes-only (cannot reproduce real API semantics, admission, and watch behaviour) | Accepted |
| [0011](0011-kubernetes-integration.md) | Kubernetes integration: client-go behind the seam, workload-typed resources — two settled questions. **How the adapter talks to the cluster:** the real `controlplane.Kubernetes` adapter uses `k8s.io/client-go`, the operator uses `sigs.k8s.io/controller-runtime`, and Burrow **never shells out to `kubectl`**. Those dependencies live *only* behind the seam — only `controlplane/internal` and `operator/` import them — so core logic depends on the interface, stays deterministic and fake-tested, and client-go's large dependency tree stays out of the client surface; it is a deliberate, justified exception to the small-dependency-graph rule, like the Postgres driver. **What a deploy creates:** the seam speaks a **workload** vocabulary rather than a Deployment one — `WorkloadKind` (empty means `Deployment`), `WorkloadSpec` with a `Kind` field, and `ApplyWorkload` / `WorkloadStatus` / `ScaleWorkload` / `DeleteWorkload` / `Logs` — even though **v0.1 uses `Kind: Deployment` exclusively** and `DeployRequest` does not expose a workload type. The reason the generalization is paid up front rather than deferred: StatefulSets are a **roadmapped certainty**, not a speculative need, so the rename is a known future cost; and when stateful workloads arrive the engine maps a workload *type* to a kind rather than letting the agent name raw Kubernetes resources. Rejects shelling out to `kubectl` (fragile string output, no typed errors, an extra binary prerequisite, worse testability), raw REST against the API server (reimplements auth, discovery, encoding, retries), importing client-go into core logic, and keeping the seam Deployment-shaped to generalize later (YAGNI would win only if stateful support were speculative) | Accepted |
| [0012](0012-in-cluster-postgres.md) | Control-plane state runs in an in-cluster Postgres (no external managed database) — the control plane's own durable state (deploy records and rollback handles, later the policy, audit, registry, and environment tables) lives in a Postgres running **in the user's own cluster**, correcting an initial stack choice that named an external managed Postgres. The reasoning is that a self-hosted control plane depending on a SaaS database contradicts what Burrow is: it makes the control plane reach *out* of the cluster to operate, requires a third-party account and credential on top of the cluster the user already runs, and couples availability and data residency to a service outside the user's control. The adapter (built on pgx) is database-host-agnostic and speaks to any Postgres via a DSN — the *deployment* is what is in-cluster — with a **supported floor of Postgres 14+**, because it uses only long-stable SQL (`JSONB`, `BIGSERIAL`, `ON CONFLICT`, `TIMESTAMPTZ`); CI runs the integration tests against a Postgres service container and local development points `BURROW_TEST_DATABASE_URL` at any instance, never a hosted service. It generalizes into a standing principle: **Burrow's core operating dependencies run in-cluster**, with an optional outbound side channel such as transactional email explicitly out of scope of the rule. Rejects an external managed Postgres (egress, a required SaaS credential, availability and residency coupling outside the user's cluster) and an embedded single-file store such as SQLite (gives up concurrent access across control-plane replicas and a cluster-grade operational story) | Accepted |
| [0013](0013-database-migrations-and-upgrade-policy.md) | Database migrations (embedded goose) and the single-minor-step upgrade policy — schema changes are **ordered goose SQL migrations embedded in the binary** (`embed.FS`, `controlplane/postgres/migrations/*.sql`) applied on startup through goose's programmatic provider API, with concurrent replicas serialized by a **Postgres advisory session lock** so one instance migrates and the others no-op; goose is used as a library, chosen over a hand-rolled migrator specifically for that ordered-versioning-plus-advisory-lock pair. The driver is **pgx through `database/sql`** (not `pgxpool`, not `lib/pq`), so goose and the application share one `*sql.DB`. Migrations are forward-only in production. The upgrade policy is **one minor version at a time within a major**, *enforced* rather than documented: a single-row `burrow_meta` table records the version that last migrated the database, and on startup the binary refuses a skip, a downgrade, or a cross-major move with an actionable error ("database is at v0.1, this binary is v0.3; install v0.2 first"); a fresh database is a first install and is not gated. The policy's stated purpose is to bound the tested surface and to keep a future **migration squash** into a baseline safe. **The upgrade-policy half is superseded by [ADR-0055](0055-multi-version-upgrades.md)** — multi-minor forward jumps are allowed, and squashing is preserved by a version floor instead — **but ADR-0055 is 🟡 Proposed and unbuilt, so the single-minor-step gate described here is what the code enforces today** ([docs/CAPABILITIES.md](../CAPABILITIES.md)); the **migration mechanism (goose, the advisory lock, pgx-via-`database/sql`, forward-only, the `burrow_meta` stamp) remains in force**. Rejects a `CREATE TABLE IF NOT EXISTS` schema const, a hand-rolled migrator, the lib/pq driver, native pgxpool, and allowing multi-version jumps | Accepted |
| [0014](0014-self-host-connectivity-via-kubeconfig.md) | Self-host connectivity via the developer's kubeconfig and the API-server proxy — burrowd runs as a ClusterIP service inside the cluster and is not directly reachable from a laptop, but the **Kubernetes API server can proxy to in-cluster services** (`/api/v1/namespaces/<ns>/services/<svc>:<port>/proxy/<path>`), and the developer's kubeconfig already authenticates to it. So self-host "connectivity" is **the kubeconfig the developer already has** — no Burrow login, no port-forward, no ingress (verified against a real DigitalOcean cluster). This brushes against [ADR-0005](0005-mcp-server-holds-no-cluster-credentials.md) and **refines rather than supersedes it**: the local client may use the ambient kubeconfig *solely for connectivity* and to read the API token from the install Secret, while the cluster-**operating** credential and the guardrails stay in the control plane, and the client acts with the developer's own access rather than a credential minted for the agent layer. A small `connect` package resolves the proxy base URL and returns a ready client, with explicit `--control-plane` / `--token` retained for an ingress or CI, and two gates protect burrowd: RBAC on the `services/proxy` subresource, and burrowd's own token check. **Its "the client sends both headers" consequence is corrected by [ADR-0015](0015-token-header-only-x-burrow-token.md)**, and its ambient-kubeconfig premise is tightened by [ADR-0038](0038-scoped-agent-credential.md). Rejects `kubectl port-forward` in a second shell (a second long-lived process to manage), putting deploy logic and the kubeconfig directly in the client (collapses the trust boundary — the kubeconfig is for *reaching* the control plane, not for *being* it), and requiring an ingress or LoadBalancer in v0.1 | Accepted (token-header detail corrected by ADR-0015) |
| [0015](0015-token-header-only-x-burrow-token.md) | Burrow's token is sent only in `X-Burrow-Token` (corrects ADR-0014) — [ADR-0014](0014-self-host-connectivity-via-kubeconfig.md) had the client set the token in **both** `Authorization: Bearer` and `X-Burrow-Token`, reasoning that the API-server transport owned `Authorization` on the proxy path. That reasoning was wrong and produced a real failure: **client-go's authentication round trippers — for both a bearer token and an `exec` credential plugin — do not overwrite an `Authorization` header that is already set**, they defer to it. So the kubeconfig credential was never applied, the API server received Burrow's token as its own credential, and the request came back `401` as a Kubernetes `Status` object before ever reaching burrowd. The bug was **masked in CI because k3d authenticates with a client certificate (mTLS)**, where no `Authorization` header is involved, and was exposed only by the reference DigitalOcean cluster's `exec`/token kubeconfig — the concrete value of the real-cluster smoke test. Decision: Burrow's clients set **only** `X-Burrow-Token`, never `Authorization`; leaving it unset lets the kubeconfig transport authenticate normally, and the API server forwards the custom header unchanged, while the same single header also works on the direct/ingress path. burrowd still accepts `Authorization: Bearer` as a fallback for third-party tools, but Burrow's own client never sets it, asserted by a regression test. Rejects stripping the token from `Authorization` only on the proxy path (makes client behaviour depend on how it was constructed, for no gain) and having the client deliberately overwrite the kubeconfig credential | Accepted |
| [0016](0016-cli-distribution-and-upgrade-lifecycle.md) | CLI distribution via Homebrew and the CLI-driven upgrade lifecycle — two questions with one answer: **the CLI is the unit a user installs and upgrades, and the CLI's version pins the control-plane version it installs**, so "upgrade the cluster" becomes "get a newer CLI, then have it roll the control plane forward," keeping one source of truth for what-goes-with-what and avoiding a separate cluster-side updater. Near term (decided): **`burrow upgrade`** performs an in-place, **state-preserving** upgrade — it reads the existing install Secrets (`burrowd-api-token`, `burrowd-db`) and reuses them, re-renders the manifests with the new `burrowd` image, and rolls **only the burrowd Deployment**, leaving Postgres and its PersistentVolumeClaim untouched; the target defaults to the CLI's own pinned `defaultBurrowdImage` constant; schema migrations ride along behind the startup gate ([ADR-0013](0013-database-migrations-and-upgrade-policy.md)); and **`burrow install` refuses to run over an existing install**, closing the secret-clobbering footgun where a re-run minted a fresh database password that no longer matched the existing volume, so the only "upgrade" was deleting the namespace and losing all control-plane state. Later (the part held at Proposed): a **Homebrew tap** plus signed release binaries, making the update path `brew upgrade burrow` → `burrow upgrade`, with the release-engineering cost (a tap repository, formula updates, macOS notarization) the stated reason it is not taken on with v0.1.x. Rejects a separate in-cluster self-updater (splits the version source of truth and puts upgrade orchestration inside the component being upgraded), making `install` idempotent and reusing it for upgrades (the exact verb ambiguity that caused the footgun), a `curl`-pipe-`sh` script as the headline installer, and shipping the CLI only as a container image | Accepted |
| [0017](0017-private-registry-authentication.md) | Private registry authentication via a developer-provisioned pull secret — real apps live in private registries, and the credential must not touch the agent path or force burrowd to gain `secrets` access, so registry auth is **human-provided, provisioned with the developer's kubeconfig, and consumed only by the kubelet**. `burrow registry login <host>` (or `--from-docker-config <host>` to lift an entry the developer already created) upserts a **single `dockerconfigjson` Secret named `burrow-registry`** in the app namespace — multiple registries merge into one file — and ensures the namespace's **default ServiceAccount lists it in `imagePullSecrets`**, which app Pods inherit because they set no `serviceAccountName` of their own. Because the Secret attaches to the ServiceAccount, **nothing else changes**: burrowd, its least-privilege Role, the deploy record, and the Deployment spec are untouched, and burrowd never sees the credential. The record also promotes an implicit pattern to a stated rule — **setup uses the kubeconfig; operations go through the control plane** — the boundary [ADR-0018](0018-reaching-an-app-at-a-url.md) and [ADR-0023](0023-provider-credentials.md) then build on. Noted cost: the pull secret lands on the *default* ServiceAccount, so every Pod in that namespace using it inherits the credential. [ADR-0040](0040-burrowd-never-contacts-the-registry.md) later removes the one code path that contradicted this. Rejects providing the credential over the agent channel, having burrowd create the Secret and set per-Deployment `imagePullSecrets` (requires `secrets` write plus control-plane state for which secret applies where), always referencing a conventional pull-secret name (noisy warnings for public-only users), and auto-syncing the whole `~/.docker/config.json` | Accepted |
| [0018](0018-reaching-an-app-at-a-url.md) | Reaching a deployed app at a URL — ingress, TLS, and DNS, with a reachability surface — the v0.2 design, built in stages. Reachability is a **chain** (an ingress controller with an external address → a Service and Ingress routing host to Pods → a TLS certificate → a DNS record), and the naive "create the resources and hope" approach fails the way agents fail: when the app is unreachable the agent cannot tell **which link broke**, so it cannot reason about a fix. The spine is therefore a read-only **`reachability` report** returning the structured state of every link, each with a status and, when unsatisfied, a next action **tagged agent-fixable or human-setup**. The chain also splits across the trust boundary, which is what the tagging encodes: installing a cluster-wide controller and handing over a DNS credential are privileged human setup (`burrow ingress install`, which **detects an existing controller or cert-manager and uses it** rather than installing a second; `burrow dns configure <provider>`), while creating an Ingress or a DNS record for an already-configured domain is a guarded operation the agent drives (`expose`/`unexpose`, `domain add`/`remove`). TLS prefers the **DNS-01** challenge when a provider is configured (it issues before the host is publicly reachable) and otherwise falls back to HTTP-01; DNS writes are scoped to delegated domains, with deletes separately gated. **§5's "credential injected into burrowd via the pod spec" is refined by [ADR-0023](0023-provider-credentials.md)** (one `burrow-credentials` Secret read through a `resourceNames`-scoped `get`, with the registry in the database), and [ADR-0022](0022-routing-backend-and-supported-kubernetes.md), [ADR-0041](0041-flatten-path-to-a-reachable-app.md), and [ADR-0042](0042-use-existing-ingress-controller.md) refine the rest. Rejects folding the controller into `burrow install`, letting the agent install it or hold the DNS credential, creating resources without a reachability surface, a LoadBalancer Service per app, manual-DNS-only, and Gateway API for v0.2 | Accepted (credential detail refined by ADR-0023) |
| [0019](0019-cli-framework-cobra.md) | Cobra for the CLI command framework; stdlib tabwriter for output — the CLI had rolled its own handling (the standard `flag` package, a hand-rolled `splitArgs` "positionals first, then flags" convention, manual per-group dispatch), which carried a flat v0.1 verb set but does not scale to the v0.2 tree (`expose`/`unexpose`, `domain add`/`remove`, `dns configure`, `ingress install`, `guard list`/`set`), where every level means hand-written help and argument handling and the bespoke flag ordering diverges from what users expect. **Adopt [spf13/cobra](https://github.com/spf13/cobra)** for commands, nested subcommands, pflag parsing, generated help, and shell completion: it is the de-facto standard in the Kubernetes ecosystem Burrow already lives in, is **Apache-2.0** (so no license-boundary concern under the then-current split), and directly retires the bespoke dispatch — the one dependency whose job it does better than the project can. **Keep human-readable output on the standard library** (`text/tabwriter` plus the existing `--json`); no table or styling dependency is taken on until an interactive or richly-formatted surface actually warrants it. Migration preserves the `run(ctx, args, stdout, stderr)` test entry point by having it build and execute the cobra root, so existing CLI tests keep working, and it lands **before** the v0.2 surface is built so new commands are never hand-rolled and then rewritten. Consequence: pflag interleaves flags and positionals, so the `splitArgs` rule is retired. Rejects continuing to roll its own, urfave/cli (weaker shell completion and far less ubiquitous in this ecosystem), a table or styling library now, and shipping a foundational CLI dependency choice without an ADR | Accepted |
| [0020](0020-guardrails-as-configurable-policy.md) | Guardrails as inspectable, configurable policy with a `confirm` disposition — refines [ADR-0006](0006-guardrails-in-the-control-plane.md), which fixed *where* guardrails live, by generalizing two compiled-in booleans (`Policy{MaxReplicas, AllowScaleToZero}`) into **entries keyed by a stable action code** (`scale.to_zero`, `scale.ceiling`, `deploy.over_newer`, later `dns.write`, `dns.delete`, `expose.public`, `app.run`, …), each carrying a **disposition — allow, confirm, or deny — and, where parameterized, a threshold**. Policy lives in the control plane's Postgres, seeded from conservative defaults by a migration, and evaluation returns one of three structured outcomes rather than a bare allow/deny. The `confirm` disposition is where the record is deliberately, unusually honest about enforcement strength: **`deny` is absolute; `confirm` is a hard gate for a human at the CLI (a person types `--confirm`) but only a *cooperative* gate for an agent**, whose tool contract requires it to surface the confirmation and re-invoke on the user's say-so — it is not cryptographically enforced, so an action that must hold against a misbehaving agent is left at `deny`, and the human's out-of-band `guard set` is the lever that relaxes it. The split over who may change policy is structural: **`guard list` is read-only and exposed to the agent** so it knows the rules before acting, while **`guard set` is CLI-only and never an agent tool** — the agent must not be able to edit its own guardrails. [ADR-0021](0021-guardrails-require-control-plane-only-agent-access.md) then bounds how far that protection actually reaches. Rejects more compiled-in booleans, exposing `guard set` to the agent, an agent-proof cryptographic confirm for v0.x, and storing policy in a ConfigMap (a second source of truth and a second RBAC surface) | Accepted |
| [0021](0021-guardrails-require-control-plane-only-agent-access.md) | Guardrails bound the control-plane path; the operator restricts the agent's other paths — the honest limit on [ADR-0020](0020-guardrails-as-configurable-policy.md). Real coding agents run with a **shell**, so an agent can invoke the `burrow` CLI directly (including `guard set`, defeating the tool-surface restriction) or use `kubectl` with the developer's ambient kubeconfig and never touch burrowd at all. Burrow cannot close this from the inside — it governs what flows through its control plane and has no control over the agent's other tools or the developer's kubeconfig. So the decision is to **state it plainly: the guardrails are a real boundary only when the control plane is the agent's sole path to the cluster, and making that true is the operator's responsibility, enforced at the agent's own permission layer**. The recommended posture is to deny the agent the `burrow` CLI, deny direct cluster tooling (`kubectl`, `helm`, anything using the kubeconfig), and allow image build and push — with the mechanism differing per agent CLI, so Burrow documents the principle and one worked example in [docs/HARDENING.md](../HARDENING.md) rather than shipping a permission file that would be wrong for most users. The framing is **defense in depth for cooperative agents**: a real boundary for an over-eager assistant that honours its configuration, not a sandbox against a malicious one, and the docs must not imply otherwise. **[ADR-0038](0038-scoped-agent-credential.md) later supplies what this record could only ask for** — a minted, burrowd-only credential that makes the guardrails binding by construction rather than by shell-denies. Rejects enforcing this from inside Burrow (impossible), exposing `guard set` behind its own confirm gate, removing the human's `guard set` CLI, and shipping a hardened agent permission file in the repo | Accepted |
| [0022](0022-routing-backend-and-supported-kubernetes.md) | HTTP routing via a shared ingress (Ingress now, Gateway-ready) and supported Kubernetes versions — three refinements to [ADR-0018](0018-reaching-an-app-at-a-url.md). **Route through a single shared ingress controller, not a load balancer per app:** a `type: LoadBalancer` Service provisions one cloud load balancer each — a flat monthly fee per app on the reference target — while one controller owns one load balancer and routes every exposed host by Host header, which is why `expose` creates a ClusterIP Service plus an Ingress. **Backend: `networking.k8s.io/v1` Ingress now, Gateway API behind the same seam later** — Ingress is GA and present on every cluster since 1.19 with no CRDs to install, whereas Gateway API needs its CRDs *and* a Gateway-capable controller first; the `expose` operation and the reachability surface are the abstraction the user and agent see, so nobody types "Ingress" or "Gateway" and the backend can be swapped without breaking them. **Supported versions:** the upstream-supported window (roughly the latest three minors) plus whatever the reference cloud offers, with **`networking.k8s.io/v1` (Kubernetes 1.19+) as the hard API floor**, documented and revisited each release. It also fixes a requirement on output: reachability must carry **two altitudes from the same facts** — a one-line, Kubernetes-free verdict for a novice, and the full structured chain for an advanced user or the agent — with the agent gauging which to surface. **[ADR-0042](0042-use-existing-ingress-controller.md) extends "detect what the cluster supports" from the Ingress-versus-Gateway axis to the which-controller axis.** Rejects a LoadBalancer per app, Gateway API as the v0.2 backend, exposing the backend choice in the default path, and supporting pre-1.19 `v1beta1` Ingress | Accepted |
| [0023](0023-provider-credentials.md) | Provider credentials — a registry of vendor tokens in one scoped Secret — the general mechanism behind "call a third-party API on the user's behalf," shaped by three forces: a credential is per-**provider** (users split compute and DNS across vendors, so a DigitalOcean-only DNS token is the wrong unit), adding or rotating a token must not restart burrowd, and burrowd stays least-privilege. Decision: **one `burrow-credentials` Secret in the control-plane namespace holds every vendor token** (one key per provider, an opaque bag carrying no schema); **the database is the registry** (a `providers` table records which providers are configured, their type, their capabilities, and which Secret key holds the token — meaning and structure live where they version cleanly, never in an implicit file or env layout); and **burrowd reads a token through the Kubernetes secrets API at the moment it makes a provider call**, so a rotated token is picked up with no restart. The permission that buys is the tightest grant Kubernetes allows: `get` on `secrets` with `resourceNames: ["burrow-credentials"]` in its own namespace — not the database secret, not the API-token secret, and it cannot even `list`; burrowd never writes the Secret. `burrow provider add <type> --token-stdin` validates the token with a test call before saving, so a bad token fails immediately rather than at first use. A future caveat is recorded: this is read-only for **static** tokens, and an OAuth/refresh-token provider would need write access. **The kubeconfig-direct write is superseded by [ADR-0030](0030-credentials-through-the-control-plane.md)** (values may traverse burrowd's authenticated API, still never the agent channel); the registry-of-tokens model and the scoped Secret stand. Rejects injecting the token as an env var (a restart per add and rotation, a Deployment patch per provider), mounting the Secret as a volume (an implicit, unversioned on-disk schema), hardcoding one DNS vendor, and storing tokens in the database | Accepted (credential transport superseded by ADR-0030) |
| [0024](0024-cli-command-taxonomy.md) | Noun-grouped CLI command taxonomy — the CLI had grown into a flat list of roughly fifteen peer verbs that conveys nothing about what goes with what, and two observations sharpen the fix: **the CLI is the human's surgeon and backstop, not the main path** (the agent does the bulk of the work), so it should be organized around the task a person is trying to do, and **following kubectl's vocabulary blindly hurts** — `expose` is a term even heavy Kubernetes users rarely touch, and it reads as vague and slightly dangerous rather than "make my app reachable." Decision: group into **nouns named for the task** while keeping a few genuinely top-level verbs — `app` (deploy, status, logs, rollback, scale, delete, `publish`/`unpublish`, `domain add`/`remove`), `config` (the credentials Burrow uses on the user's behalf: `provider`, `registry`), `system` (cluster-wide infrastructure: `ingress`), with `install`/`upgrade`, `guard`, and `version` staying flat. Specific calls: **`expose` → `publish`** and `unexpose` → `unpublish` (the wire API and `controlplane.ExposeSpec` keep their names — only the user-facing verb changes); `domain` lives under `app` because it is the DNS half of the same reachability chain; `reachability` folds into `app status`; `guard` stays top level because policy spans every operation. **The agent's tool surface stays flat** — grouping is a human-discoverability aid, and an agent selects by name and description rather than browsing a menu. A deliberate breaking change made pre-1.0 while the surface is small and the regroup is mechanical under Cobra. **Extended by [ADR-0037](0037-cli-onboarding-and-organization.md)** (which drops `system` into `cluster`), [ADR-0054](0054-install-is-control-plane-only.md), and [ADR-0060](0060-cluster-lifecycle-command-group.md) (which moves `install`/`upgrade` under `cluster`). Rejects the flat verb list, full kubectl-style noun-verb everywhere, and keeping `expose` | Accepted |
| [0025](0025-building-block-addons.md) | Building-block add-ons — a curated catalog of vetted, self-hostable backing services — the model behind "the agent writes the app-side integration code and Burrow deploys and operates the backing service." Two layers mirroring the provider registry: a **compiled-in, curated catalog** where each entry is an `AddonSpec` naming the workload to create, a **pinned image**, sane defaults, and the **connection contract** (the host, port, and Secret keys an app uses), and a **DB-backed registry of instances** recording what is actually installed on this cluster. Add-ons deploy through the **existing Kubernetes seam apps already use, not Helm**, keeping full control, the guardrail gate, and the in-memory fakes the tests rely on, with the pinned-manifest path reserved for the rare add-on that genuinely needs it. Two constraints shape everything: a **firm license bar — Apache / MIT / BSD only** for anything Burrow *ships*, which excludes Loki, Grafana, and Tempo (AGPL) and Elasticsearch (SSPL) and is what steers the picks to the Victoria stack and ValKey; and credentials never crossing the agent channel — install writes a generated password into a cluster Secret and returns only the **non-secret** connection details plus the Secret's *name*, never its value. Add-ons are **app-facing** (the app connects to it) or **agent-facing** (the agent queries it to answer questions), and Burrow deliberately **bundles no dashboard UI** — Grafana is AGPL, and the evidence is that users want answers, not dashboards, so the agent is the query layer. Install and remove are confirm-by-default, which the record frames as an *adoption* mechanism as much as a safety one: operators say plainly they will not let an agent change production without a human in the loop. **Refined by [ADR-0026](0026-observability-query-adapters.md)** (install *or* connect). Rejects Helm-based install, a generic deploy-any-image-as-a-service, agent-chosen backing services (defeats the license bar and the vetted-defaults promise), and per-type verbs | Accepted |
| [0026](0026-observability-query-adapters.md) | Observability add-ons — query adapters over installed *or* existing backends — refines [ADR-0025](0025-building-block-addons.md) on the strength of research finding that most users already run a cluster, often already with logging and metrics, and that their number-one Day-2 pain is **troubleshooting what they already have**, not greenfield deploy. Two facts make a connect path clean: **the license bar only constrains what Burrow distributes** — querying a user's existing Loki or Datadog is fine regardless of its license — and **capability is separable from provisioning**, so "query my logs" is the value and whether the store is Burrow-installed or pre-existing is a detail behind a seam. Decision: model observability as a **capability behind a query seam with adapters**, with two registration modes in one `addon` group — **`addon install <capability>`** deploys a vetted, permissively-licensed default (logs → VictoriaLogs) **and registers it as queryable in one step** (install implies connect; the user does not run a second command), while **`addon connect <backend>`** registers an adapter to an existing backend by endpoint plus optional credential, working against AGPL and proprietary backends alike. **Capabilities are derived, not declared**: a single-capability backend implies its own (Loki → logs, Prometheus → metrics), and a multi-capability platform is **probed with the provided key** to detect what is actually accessible. `addon list` shows each entry's **mode** (installed versus connected) and its capabilities uniformly. The VictoriaLogs install is deliberately the **reference backend** — without an installed store there is nothing to exercise the query seam or a new connect adapter against — so it is the first slice and the test bed for every later adapter. Rejects install-only as written (misses the larger existing-cluster, Day-2 market and forces a redeploy to get any value), user-declared capabilities, and a separate top-level `connect` command | Accepted |
| [0027](0027-audit-log.md) | Audit log — an append-only Postgres record of agent operations and guardrail decisions — every mutation already flows through the control plane and is guarded there, but nothing **recorded** what happened, so "what did the agent do at 02:00 when the site went down?" was unanswerable from Burrow itself. Dogfooding sharpened the argument: in a live run the only thing that paused a production rollback was the *client agent's* own permission prompt, not Burrow, and no record of that decision existed anywhere — the authoritative decision is made where the credentials are, so the authoritative record belongs there too, not in an agent-specific, best-effort client log. Each row captures **when** (from the injected clock, no ambient time), **operation and target**, **arguments — redacted through a per-operation allowlist**, the **guardrail code and disposition**, the **outcome**, the **result**, and the **caller**. The outcome vocabulary is exactly what guardrail evaluation already distinguishes: `allowed`, `held` (confirmation required, not executed), `denied`, `executed` — and a held operation plus its later confirmed execution are **two rows**, so the trail reads "confirmation requested" → "confirmation received, executed." **Redaction is load-bearing**: the writer allowlists what it records and never serializes raw request bodies, or it would leak exactly the secrets kept off the agent channel. The log is append-only by contract with no edit or delete path through the API, and the agent may **read** it but never write to or alter it — it is the operator's record *of* the agent, not the agent's scratchpad. A lost row is a logged degradation, never a reason to fail a deploy. Identity is coarse (a single API token plus an optional client label) with a column reserved; [ADR-0038](0038-scoped-agent-credential.md) adds `principal` and [ADR-0039](0039-cli-control-plane-version-skew.md) the acting client version. Rejects Kubernetes Events or the Kubernetes audit log (age out, live in the user's etcd, carry none of Burrow's guardrail semantics), structured application logs, trusting the client to log itself, and an event-sourcing pipeline or external SIEM now | Accepted |
| [0028](0028-app-config-and-secrets.md) | App config and secrets — a lifecycle store, secrets off the agent channel — `app deploy --env KEY=VALUE` was both a declaration rather than a lifecycle (no way to change one variable on a running app without a full redeploy restating everything) and a mishandling of secrets (values rendered inline into the Deployment spec *and* persisted in the release record, with the deploy tool carrying `Env` so a secret would cross the agent channel). Decision: an **app-scoped store managed independently of deploy, in two parallel command groups**, and **deploy no longer takes environment at all**. `app env` values are non-secret, stored in Postgres and **rendered inline into the pod template**, which means Kubernetes performs a rolling update automatically on change — no ConfigMap and no checksum trick needed. `app secret` values live **only in a per-app Kubernetes Secret**, never inlined, never in Postgres, with Postgres recording **keys only** and `secret list` returning keys only. Because changing a value under an existing key does not mutate the pod template, Burrow forces a rollout with a **checksum annotation** over the Secret's contents (the standard Helm pattern). `--no-restart` defers the rollout so "new config plus new release" costs one restart instead of two. The per-app Secret is deliberately **the single seam for backend credentials**, so a BYO external database and a future Burrow-provisioned one both arrive as `DATABASE_URL` and the app cannot tell them apart. The record also instructs the agent never to ask a user to paste a secret value into a prompt, since prompt content is retained and re-sent on every later call. **Its kubeconfig-direct secret transport is superseded by [ADR-0029](0029-secrets-through-the-control-plane.md)**; everything else stands. Rejects keeping env on deploy, env via a ConfigMap with `envFrom` (does not auto-roll, so it would need the same checksum trick anyway), one `--secret` flag instead of two groups, and the app name as a command token (collides with reserved verbs) | Accepted (secret transport superseded by ADR-0029) |
| [0029](0029-secrets-through-the-control-plane.md) | Secrets traverse the control-plane API, never the agent channel — [ADR-0028](0028-app-config-and-secrets.md) required `app secret set` to write the value kubeconfig-direct so burrowd never saw it, and that does not generalize: **a web UI has no kubeconfig, only an authenticated session to burrowd**, and the same is true of a managed control plane. Re-examining the trade settles it: burrowd is the **designated trust boundary**, already holds the API token, the database password, and provider credentials, already operates the app namespace with full workload CRUD, and its RBAC is **entirely namespaced Roles with no `ClusterRole`** — so routing a value *through* it stays inside its existing scope and does not widen its reach, whereas the kubeconfig-only rule bought little and cost a second, special-cased secret-writing path. Decision: a secret value **may traverse burrowd's authenticated API** (`POST /v1/apps/{app}/secrets`) and burrowd writes the per-app Secret; the CLI drops its kubeconfig-direct write. **The agent boundary is unchanged and is the line that actually matters**: there is no secret-set agent tool (a test fails if one appears), while `secret list` (keys) and `secret unset` (by key) stay agent-callable because they carry no value. Two guards are called out as load-bearing and therefore tested rather than assumed: **never logged, never audited** (the request log skips the body and the audit records the key name only), and only over the authenticated, TLS-protected API. A complementary mitigation moves the default app namespace from `default` to a dedicated **`burrow-apps`**, so the namespace-scoped secrets grant does not land on the cluster's shared namespace. The record names what it gives up: the strict "burrowd never sees a value" property of ADR-0028, which did not survive contact with the UI and managed requirements. Rejects keeping kubeconfig-only, a separate secrets service or external vault now, and inlining values into the Deployment or storing them in Postgres | Accepted |
| [0030](0030-credentials-through-the-control-plane.md) | Burrow-owned credentials traverse the control-plane API, never the agent channel — extends [ADR-0029](0029-secrets-through-the-control-plane.md) from *app* secrets to Burrow's **own** vendor and connected-backend credentials (DNS provider tokens; a Loki or Prometheus bearer token from `addon connect --auth`), which [ADR-0023](0023-provider-credentials.md) had the CLI write kubeconfig-direct — the same limitation for the same reason: no kubeconfig exists in a web UI or a managed path. Decision: those values **may traverse burrowd's authenticated API**, which writes them into the `burrow-credentials` Secret; `provider add` and `addon connect --auth` drop their kubeconfig-direct writes, and **the agent never sets a credential value** — it references and lists keys only. The RBAC change is one verb: burrowd's grant on `burrow-credentials` broadens from `get` to **`get, update`**, still **`resourceNames`-scoped to that one named Secret** in the control-plane namespace, with no `ClusterRole` — which the record notes is *tighter* than the app-namespace secrets grant, because RBAC cannot name-scope that one. The same never-logged, never-in-an-error, never-in-a-response, never-over-the-agent-channel guards are named as the real risk surface. It also answers "how do we manage secrets for add-ons?": Burrow **operates** add-ons with safe defaults, so they get no `app secret`-style store — the only user-provided add-on secret is the connect-time token, which rides this path, and generated credentials (a future Postgres password) are burrowd-generated and never typed by the user. The registry pull secret ([ADR-0017](0017-private-registry-authentication.md)) follows the same principle but is a different secret type in a different namespace and is deferred. Rejects keeping kubeconfig-direct, a parallel `addon secret` / `addon config` store (would invert the safe-defaults model), and an external vault now | Accepted |
| [0031](0031-postgres-addon.md) | The Postgres add-on — one shared instance, a database and role per app — the first **backend** building block, extending the add-on model from observability into the data tier. `addon install postgres` deploys the official `postgres` image (PostgreSQL License, BSD-style, clearing the [ADR-0025](0025-building-block-addons.md) license bar) as a `Recreate` Deployment plus Service plus PersistentVolumeClaim in `burrow-addons`, with **burrowd generating the superuser password at install** into a `burrow-postgres` Secret the pod reads via `secretKeyRef` — generated server-side, never returned by an API, never logged, never over the agent channel — living in `burrow-addons` rather than the control-plane credentials Secret because a pod can only mount a Secret in its own namespace. `addon attach postgres <app>` then runs idempotent admin SQL as that superuser: `CREATE ROLE app_<app>`, `CREATE DATABASE <app> OWNER app_<app>`, **`REVOKE CONNECT … FROM PUBLIC`**, `GRANT CONNECT … TO app_<app>` — the constraint that makes a *shared server* still mean **real isolation**, because sharing a server is fine and sharing a database is not, and the expensive thing is instances, not databases. It composes `DATABASE_URL` and writes it into the app's per-app Secret through the [ADR-0029](0029-secrets-through-the-control-plane.md) path, then restarts the app so `envFrom` picks it up. Identifiers are validated against `[a-z][a-z0-9-]*` and quoted before reaching SQL; re-attach rotates the password rather than erroring. **Attach is safe to expose to the agent precisely because the agent supplies only an app name** and no value ever crosses. burrowd's `burrow-addons` Role gains its first secrets grant, namespace-scoped, still with no `ClusterRole`. Rejects a dedicated instance per app by default (a pod and a volume each — deferred to an explicit opt-in for a real failure-domain need), one shared database with a schema or just a role per app (a missed `REVOKE` leaks another app's tables), the agent supplying the password, storing `DATABASE_URL` in the control-plane database, and an AGPL or proprietary distribution | Accepted |
| [0032](0032-postgres-backups.md) | Postgres backups — logical dumps via Jobs, recorded in the registry — a database an operator cannot back up is one they cannot trust with real data, so backup becomes a one-ask operation shaped by four forces. Dumps are large and slow, so streaming `pg_dump` through burrowd would tie up the control plane and force a Postgres client binary into its image — a backup is naturally a **short-lived Kubernetes Job running the `postgres` image** (which already carries `pg_dump`/`pg_restore`) in the add-on namespace, next to the instance. A backup needs a durable home and the object-store add-on does not exist yet, so a dedicated **backup PVC** is the available target, with an `--to s3://…` seam left open. **"What backups do I have?" is answered from the control-plane database, not by scraping the volume** — burrowd is not mounted to the PVC, and recording metadata when the Job succeeds is the versioned contract, consistent with keeping the registry in Postgres rather than scraping the cluster. And restore is destructive, so it sits behind a **confirm guardrail** and is audited. Concretely: `addon backup postgres <app>` writes `-Fc` output to `/<pvc>/<app>/<id>.dump` and records a `postgres_backups` row (id, app, time, size, path, status); `addon backups` lists from that table; `addon restore postgres <app> --backup <id>` runs `pg_restore --clean --if-exists`. The Jobs receive the superuser password only via `secretKeyRef` env, never as an argument or in a log. burrowd's `burrow-addons` Role gains `batch/jobs` (and `cronjobs` for the follow-on), still namespace-scoped. A named follow-on slice covers scheduled backups and `keep last N` retention; the whole design is recorded here so the follow-on adds no new decision. **The Job lifecycle established here is reused by [ADR-0048](0048-one-off-command-runner.md).** Rejects streaming through burrowd, `pg_dumpall` as the primary granularity (per-app dumps match the database-per-app isolation model), listing by reading the PVC, object storage as the first target, and an unguarded restore | Accepted |
| [0033](0033-relicense-to-apache.md) | Relicense the whole repository to Apache-2.0 — **supersedes the license decision of [ADR-0001](0001-license-and-dco.md)**: the per-package split (Apache client surface, FSL-1.1-ALv2 control plane and operator) is replaced by a single **Apache-2.0** across the entire repository, with every SPDX header rewritten, the FSL `LICENSE` files removed, and the CI check enforcing the uniform header. Four findings changed the calculus. **Adoption is the bottleneck, not monetization-defense** — a pre-adoption self-host tool dies from not being tried, not from being strip-mined. **The target user reflexively avoids non-OSI and copyleft licenses**: issue-mining of comparable self-host PaaS projects and community sentiment show source-available licenses read as "not real open source" and as a bait-and-switch risk, and AGPL as a corporate hard-ban, while Apache "removes all friction for adoption." The record's sharpest line is that **perception governs adoption, not the license's actual text**. **Permissive does not preclude the business** — Apache-licensed peers run substantial businesses, and enterprises pay for risk transfer (SLAs, SOC 2, legal liability, support), not for core features behind a license wall. And **the strip-mine threat is scale-stage**, appearing only after a project has won, so there is no adoption tax worth paying now for it. Monetization moves off the license: a hosted offering, an enterprise tier (SSO/SAML, teams, advanced RBAC, compliance) kept out of this repo, and risk transfer — with the **agent-safety core (guardrails, audit, least-privilege, secrets handling) staying in the open Apache code and never gated**, because it is the differentiation and the trust. ADR-0001's DCO, CLA-gating, and sole-ownership stance is explicitly unchanged. Rejects keeping FSL-1.1-ALv2 (which converts to Apache in two years regardless, so this largely arrives at the same endpoint sooner) and AGPL-3.0 with a commercial dual-license | Accepted |
| [0034](0034-agent-native-onboarding.md) | Agent-native onboarding — install probes the cluster, capabilities are read live, provisioning is demand-driven and cost-aware — competitive research found the self-host-PaaS adoption ceiling is the **install funnel**, not cluster ownership: the closest Kubernetes-native analog has an order of magnitude fewer stars than the VPS-based incumbents, and nearly every high-engagement issue is an install that died on a Kubernetes prerequisite *for a user who already had a cluster* — ingress-controller confusion, DNS-to-LoadBalancer wiring, cert-manager serving a *fake* certificate, a default StorageClass that does not bind, and a **cluster-type picker whose every branch fails differently**. Decision, in four parts. **Detection belongs in `install`**, which already holds full cluster visibility at that moment, and it prints a plain-language summary of what it found. **Capabilities are read live for the agent, never from a snapshot cached at install**, because a user can `kubectl apply` a controller afterwards and a stale profile goes wrong exactly when it matters — which costs burrowd **one narrow read-only `ClusterRole`**: `get`/`list` on `nodes`, `storageclasses`, `ingressclasses`, plus API-group discovery (which needs no RBAC and is how cert-manager is detected, by its CRDs). No secrets, no writes, no other resources; burrowd's *write* surface stays exactly as namespaced as before. **The read-only capability surface replaces the cluster-type picker** — Burrow observes the cluster instead of asking the user to classify it — and is the low-trust agent entry point, a survey before anything is changed. **Provisioning is demand-driven, kubeconfig-side, and cost-aware**: nothing cluster-wide is installed without an explicit yes, a step that creates a **billable** cloud load balancer says so and names the provider (flagging the *class* of cost, never a hardcoded price, which drifts), and a free alternative is offered where one exists. Onboarding finishes by walking the reachability chain so "live at `https://…`" is *verified*. Rejects a separate `burrow doctor` verb, a cached capability snapshot, a cluster-type picker, assuming every cluster wants ingress, provisioning cluster-wide substrate through burrowd, and auto-provisioning without confirmation | Accepted |
| [0035](0035-environments.md) | Environments — context-routed clusters, then namespace-scoped environments — the direct answer to the loudest reason self-hosters resist an agent: *don't let it touch prod*. An **environment is a kubeconfig context (a cluster) plus a namespace plus a guardrail policy**, and real setups sit at two ends of that line, so it ships in two phases because the mechanism differs sharply. **Phase 1, cluster-per-env via context routing:** the kubeconfig contexts *are* the environments — no new registry, nothing to manage — with `connect.Options` gaining a `Context` field wired into `ConfigOverrides.CurrentContext`, a global `--context` flag, and per-call routing for the agent through a client-per-context factory (the one structural change), each client reading its own cluster's token. Per-environment guardrails come **for free** here, because each cluster's burrowd already holds its own policy: lock down prod's and leave staging's permissive, with no new mechanism. **Phase 2, namespace-per-env within one cluster:** an environment registry in burrowd's Postgres maps a name to a namespace with its own policy; `burrow env add` is a kubeconfig-side setup operation that creates the namespace and grants burrowd a Role there; apps and their per-app Secrets become per-environment; and guardrail codes gain the environment as a prefix (`prod.app.delete`). A single implicit `default` environment preserves today's behaviour, so multi-environment is opt-in with no regression. Two constraints hold across both phases: **the agent must explicitly name the environment to act on it**, and **posture is never inferred from the name** — a guardrail is policy, not magic. **Its `burrow context` surface is superseded by [ADR-0036](0036-environment-selection.md)**, and [ADR-0047](0047-agent-environment-safety.md) turns the explicit-targeting rule into a code-level forcing function. Rejects a central multi-cluster control plane (a far larger credential blast radius), a client-side env-to-context alias file in phase 1, name-inferred strictness, and one global policy across environments | Accepted |
| [0036](0036-environment-selection.md) | Environment selection — one `burrow env` surface that follows the kube context — [ADR-0035](0035-environments.md) shipped environments in two shapes plus a `burrow context list` command and a contexts tool that predated `burrow env`, and shipping both invites the obvious "context or env, which do I use?" confusion. The target user's real workflow resolves it: multiple clusters switched with `kubectx`, names that vary by org and change over time (so **names are the user's, never inferred**), and a namespace pinned with `kubens` only sometimes. A "burrow environment" in the user's head already *is* `{kube context, namespace}` steered with the standard tools, so Burrow rides that and exposes one concept. Decision: **`burrow env` is the single surface**, an environment is a user-named handle resolving to `{context, control-plane-namespace, app-namespace}` — the control-plane-namespace dimension carried from day one so a future control-plane-per-team is a non-default value rather than a breaking change. Handles live **client-side** in `~/.burrow/config` (YAML with an `apiVersion`/`kind` header, overridable by `$BURROW_CONFIG`), because a handle can span clusters while burrowd is per-cluster, so no single control plane can hold the map; this is human selector state like the kubeconfig, and a deliberate exception to "no files to manage." `burrow env use` pins, `burrow env follow` returns to tracking, `burrow env add` also performs the phase-2 server-side setup, and `burrow scan` walks every context probing for burrowd. **The default is to follow the current kube context, and the resolved target is printed on every command** — the failure mode that makes following risky is removed by making the target legible, not by refusing to follow. The agent targets by explicit named argument and never rides the human's pin, with every result echoing the environment it acted in. Rejects parallel context and env commands, pinned-by-default (drifts from the context the operator already steers), a server-side registry for a cross-cluster map, name-inferred posture, and following silently | Accepted |
| [0037](0037-cli-onboarding-and-organization.md) | CLI onboarding and command organization — `burrow --help` listed twelve top-level commands as a flat wall that buried the golden path, and three first-run failure modes lost users outright. Decision, in four parts. **Intent-based Cobra groups ordered along the golden path** — *Get started* (`install`, `upgrade`, `cluster`, `config`), *Environments* (`env`), *Operate* (`app`, `addon`), *Govern* (`guard`, `audit`), *Other* — so the groups themselves teach the shape of the system. **`install` becomes explicit and non-interactive**: the kube context is a **required positional argument**, so nothing is ever installed into the current context implicitly (which could be prod); with no argument it lists the contexts and tells the user to re-run — it does **not** prompt, keeping it TTY-free and therefore safe in CI and for the agent; a missing kubeconfig produces a plain explanation rather than a raw library error; and on success it names the environment (an explicit `--environment` or a generated adjective-animal handle) and **writes it into `~/.burrow/config`** so first-run detection flips. **`install` applies its manifests with client-go server-side apply instead of shelling out to `kubectl`**, removing the last external binary prerequisite and making the CLI self-contained (the shell-out was the lone outlier in a codebase already using client-go). **`system` is dropped and folded into `cluster`** — one concrete noun for both inspecting what the cluster can do and provisioning its infrastructure, since the inspect/provision split across two nouns on the same domain was the redundancy and `system` read as if it might mean the control plane. First-run config awareness leads a brand-new user to `install` and de-emphasizes the rest. Rejects defaulting `install` to the current context, an interactive TUI picker (needs a TTY, breaks CI and the agent, and adds a dependency), keeping `kubectl apply`, keeping `system` alongside `cluster`, and moving `install` under `env` | Accepted |
| [0038](0038-scoped-agent-credential.md) | Scoped agent credential — `install` mints it, the human keeps admin — the ambient kubeconfig the client used for connectivity ([ADR-0014](0014-self-host-connectivity-via-kubeconfig.md)) is real cluster access, in practice often cluster-admin, and the agent runs in the same environment with a shell, so the guardrails only *bind* if that is fixed — and [ADR-0021](0021-guardrails-require-control-plane-only-agent-access.md)'s shell-denies are defense in depth, not a boundary, because they depend on the agent honouring its permission configuration rather than on what the credential can actually do. Decision: **`burrow install` mints a scoped, burrowd-only credential** — a `burrow-agent` ServiceAccount plus a narrow Role and RoleBinding granting **only** proxy access to the burrowd Service and `get` on the `burrowd-api-token` Secret, and nothing else (no pods, no other secrets, no other namespaces, no cluster-scoped read) — and writes a **self-contained kubeconfig into `~/.burrow/`, never into `~/.kube/config`**. The consequence that matters: even a shelled-out `kubectl` pointed at it is denied everything except reaching burrowd, so **the guardrails become binding by construction**. The human keeps their own admin kubeconfig for `install`, `upgrade`, `guard set`, `env add`, and credential setup. The credential is a long-lived ServiceAccount bearer token for now, because the RBAC narrowness is the real control and it is revocable by deleting the ServiceAccount; TokenRequest and client certificates are named as future tightenings. `install` also becomes **idempotent and multi-user**: on a populated cluster it skips the cluster apply and performs a **local join**, writing that user's config without re-minting burrowd's secrets. Agents share one ServiceAccount today, because per-user ones are inseparable from SSO — so a **`principal` seam** is threaded through now as a constant (credential provisioning, caller-identity resolution, and a new `principal` audit field), so SSO attribution later **fills in a value instead of migrating the meaning of past rows**. The operational contract is [docs/HARDENING.md](../HARDENING.md). Rejects keeping the ambient admin kubeconfig, per-user ServiceAccounts now, and exposing burrowd via an ingress with a shared token | Accepted |
| [0039](0039-cli-control-plane-version-skew.md) | Version skew between the CLI and the control plane — the clients are installed and upgraded **independently** of burrowd, which runs in the cluster and moves by a separate privileged act, so every combination of newer and older occurs in normal use. Two forces make this sharper than in a single-binary tool. A burrowd upgrade is a **one-actor event with many-actor blast radius**: if it could break teammates' CLIs, every server upgrade becomes a coordinated flag-day — exactly the friction Burrow exists to remove. And a long-running agent subprocess keeps executing the old binary after a `brew upgrade` until the session restarts, so even a single user can be driving an older client mid-session than the CLI in their own terminal. Decision: **burrowd is the compatibility anchor** — it owns the API contract because it is the only stateful, credentialed layer — it stays **backward-compatible within a one-minor window** with an **additive-only** API inside a minor (a breaking change is carried across a minor with a deprecation, never made in place), and it **never hard-blocks a request on version difference alone**; refusal is reserved for a request it genuinely cannot honour and is scoped to that operation, not the session. The mechanism is a **version handshake**: the client sends `X-Burrow-Client-Version` alongside `X-Burrow-Token`, so a newer client calling a feature the server lacks gets a structured "this control plane does not support `autoscale`; ask an operator to run `burrow upgrade`" instead of an unknown-endpoint failure, and a client below the window gets "your CLI is too old; run `brew upgrade burrow`." The acting client version also joins the audit record beside the principal. **The one-minor band is widened by [ADR-0055](0055-multi-version-upgrades.md)** to the same major down to the version floor — the band was justified *by* [ADR-0013](0013-database-migrations-and-upgrade-policy.md)'s single-minor-step rule, so the two move together — **but ADR-0055 is 🟡 Proposed and unbuilt, so burrowd still serves one minor back**; the anchor, no-hard-block, handshake, and additive-API guarantees stand. Rejects a hard version lock, lockstep, the client as anchor, and advisory-only with no handshake | Accepted |
| [0040](0040-burrowd-never-contacts-the-registry.md) | Burrowd never contacts the registry; Kubernetes resolves and pulls — the deploy path had grown a `registry.Resolve` step doing a `remote.Head` against the registry with go-containerregistry's default keychain, purely to record a content digest on the release. That digest was **never used**: the workload is applied by image *reference*, and rollback redeploys by reference too. So burrowd contacted the registry solely to populate an audit field it never acted on — a latent contradiction of [ADR-0017](0017-private-registry-authentication.md) ("the credential is consumed only by the kubelet, never by burrowd," which also deliberately has no `secrets` access) and the direct cause of a real bug: for a **public** image the anonymous `HEAD` succeeded so nobody noticed, but for a **private** image it returned `401`, surfaced as HTTP 500 — meaning an image the kubelet could happily *pull* could never be *deployed*. Decision: **burrowd never contacts a registry, for any reason.** The resolver, the `Registry` seam and its fake, and the go-containerregistry dependency are deleted; the pull secret remains the only registry credential and is consumed only by the kubelet; the release is identified by its **reference**, and the digest, if wanted, is **read back** from Kubernetes (`Pod.status.containerStatuses[].imageID`) as an observed value, never an input to deploy or rollback. The trade the record names honestly: removing the resolve removes the synchronous "image not found" check at deploy time, so a missing, mistyped, or unauthorized image now surfaces asynchronously as `ImagePullBackOff`, turned into an actionable status `issue` that distinguishes "no credential for this registry" from "image not found." The 500 disappears by removing code rather than adding credential plumbing, and the dependency graph shrinks. Rejects giving burrowd the registry credential, keeping the default-keychain resolve (works only for public images and fails silently for private ones), and deploying by digest (needs exactly the round-trip and credential this removes) | Accepted |
| [0041](0041-flatten-path-to-a-reachable-app.md) | A flatter path to a reachable app — getting an app onto the internet meant holding several order-dependent steps at once (deploy, expose, add DNS, plus one-time controller and DNS-provider setup), and the app had **no stable in-cluster Service until it was publicly exposed**, so apps could not address each other and metrics had no stable target. Four decisions. **A ClusterIP Service is created at deploy, by default** (`<app>.<namespace>.svc`), internal only, exposing nothing to the internet and costing nothing — so every app is addressable the moment it is deployed, and exposing it later only adds the Ingress, mirroring how a PaaS treats a deployed service. **The app declares its port at deploy** (an optional `port` on the deploy call): the agent already reads the serving port from the code, so it simply supplies it earlier; no port means no Service, addable on a later deploy. **Reachability collapses into one guarded publish operation** — ensure the Service, create the Ingress with TLS, write the DNS record when a provider is configured (otherwise return the external address for the user to point DNS at by hand), wait for the certificate to issue, and confirm the app answers — one intent for the agent instead of an expose-then-add-DNS sequence, with the lower-level operations kept as primitives beneath it and each link still enforcing its own guardrail. **The one-time prerequisites are surfaced, not stumbled into**: installing the controller (billable) and configuring a DNS provider (a credential) stay deliberate human steps, but publish reports them as structured, actionable prerequisites, and the capability survey must report them **accurately** — a controller must actually be running, not merely have left an IngressClass behind. Backward compatible: existing apps keep working and gain a Service when a port is next supplied. **Largely unbuilt** ([docs/CAPABILITIES.md](../CAPABILITIES.md)): `deploy` has no port and creates no Service, and `publish` neither writes DNS nor waits for the certificate — those stay `domain add` and `reachability --wait`. Rejects keeping the separate deploy/expose/add-DNS steps, inferring the port from the image's `EXPOSE` directive (frequently unset or wrong, and metadata rather than a guarantee), defaulting the port to a fixed value, and auto-exposing every app publicly on deploy | Accepted |
| [0042](0042-use-existing-ingress-controller.md) | Use the cluster's existing ingress controller — [ADR-0022](0022-routing-backend-and-supported-kubernetes.md) said Burrow "detects what the cluster supports," but the implementation went further toward ingress-nginx specifically: the install deployed it, and the exposed app's Ingress, the cert-manager HTTP-01 solver, and the capability check all **hardcoded the `nginx` class in three places**. That is fine on a bare cloud cluster where Burrow installs the controller — and wrong for **k3s and k3d, which ship traefik by default** and are what a large share of self-hosters, homelabs, and edge users run: on those clusters Burrow reported "no ingress controller" and pushed the user to install a redundant second one beside their working traefik. The barrier was not technical: standard `networking.k8s.io/v1` Ingress is served by every major controller, and cert-manager's HTTP-01 challenge is controller-agnostic **as long as the temporary solver Ingress names the right class**. Decision: **detect the running controller and its class** (ingress-nginx and traefik first, others best-effort), with a leftover IngressClass and no running controller still correctly reported as unusable; **bind both the app's Ingress and the HTTP-01 solver to the detected class**; **install ingress-nginx only when nothing is there**, otherwise adopt what exists (ensuring cert-manager and the ClusterIssuer wired to the existing class); and **stay portable** — controller-specific annotations are applied only when that controller is in use, never assumed. The user-facing surface does not change; nobody types "nginx" or "traefik." **Unbuilt** ([docs/CAPABILITIES.md](../CAPABILITIES.md)): the app Ingress class and the HTTP-01 solver class are still the literal `"nginx"`, capability probing recognizes ingress-nginx only, and `cluster bootstrap` disables k3s's Traefik so ingress-nginx owns ingress. The stated implementation cost, and the reason the officially supported set stays small and the default path annotation-light, is handling controller-specific behaviour per detected controller. Rejects requiring ingress-nginx, serving traefik through its `IngressRoute` CRD (native but non-portable and needs its CRDs), and auto-installing a controller the user did not ask for (it can provision a billable load balancer, so it stays a deliberate step) | Accepted |
| [0043](0043-public-reachability-is-a-loadbalancer.md) | Public reachability is a LoadBalancer, not NodePort — [ADR-0034](0034-agent-native-onboarding.md) offered a LoadBalancer-versus-NodePort choice, and the NodePort branch does not survive contact with the goal: NodePort exposes the controller on **high ports (30000-32767), not :80/:443**, and **DNS has no concept of a port** — an A record maps a name to an IP, and a browser opening `https://host` always connects on :443 — so a NodePort-exposed controller cannot serve a turnkey public site and **defeats Let's Encrypt HTTP-01**, which is fetched on :80. It is also unnecessary for internal traffic, which ClusterIP already covers. The two real needs are a public IP for public reachability and a stable in-cluster address for internal traffic; NodePort serves neither and sits confusingly between them. Decision: **expose the shared ingress controller through a `type: LoadBalancer` Service**, which is the same concept on every target — a billable cloud load balancer, k3s's built-in **servicelb** (klipper) binding node host-ports and assigning the node IP, or **MetalLB** on bare metal; **internal stays ClusterIP**; **NodePort is dropped as a user-facing choice**. The load-bearing correction is in detection: **capability detection must report LoadBalancer support from whatever actually services LoadBalancers, not only a recognized cloud `providerID`** — an ephemeral-cluster test proved a LoadBalancer received the real external address `172.18.0.2` from servicelb on k3s while detection reported it unsupported. With no provider at all, Burrow guides the user to **MetalLB**, never NodePort. **Cost disclosure narrows from "the LoadBalancer path" to "a *billable* LoadBalancer"**, so a free servicelb or MetalLB setup needs no approval gate. This is what makes the cheap single-VPS tier of [ADR-0044](0044-single-vps-k3s-cluster.md) work. Rejects keeping NodePort as the free option, binding :80/:443 via hostPort or hostNetwork (that is exactly what servicelb already manages, and leakier when exposed to users), detecting support by creating a probe LoadBalancer Service (side-effecting and slow for a read-only survey), and requiring a cloud provider for public exposure | Accepted |
| [0044](0044-single-vps-k3s-cluster.md) | Provision a single-VPS k3s cluster — a complete Burrow target for roughly the price of a $5 VPS instead of about $24/mo for a managed control plane plus a billable cloud load balancer, aimed at a large and disproportionately vocal self-hoster segment. k3s is the fit because it ships servicelb, traefik, and local-path storage in one binary, and **servicelb is the unlock**: on a single node a `type: LoadBalancer` Service is assigned the node's own IP for free (proven in an e2e; detection landed with [ADR-0043](0043-public-reachability-is-a-loadbalancer.md)), so the node IP is the public IP DNS points at, and HTTP-01 works because the node serves :80. The flow is **Option B, an installer run on the VPS**: the user opens `:6443`, `:80`, and `:443`, SSHes in once, and runs a `curl`-pipe-`sh` bootstrap that installs k3s with `--tls-san <public-ip>` and `--node-external-ip <public-ip>` (so the API-server certificate is valid for the address the laptop connects to), deploys burrowd, and prints a single **`burrow join <token>`** line carrying the API URL, the cluster CA, and an **admin** credential. Running that token on the laptop records the admin kubeconfig **and** mints the scoped agent credential, reusing the [ADR-0038](0038-scoped-agent-credential.md) join path — so **the SSH is a one-time bootstrap and every later operation, governance and agent alike, runs from the laptop**, exactly like a managed cluster. **Burrow never SSHes**: the privileged channel is the user's own session, host-key-verified with their own tooling, and no dependency is taken on k3sup, whose only useful part is a three-line kubeconfig rewrite. Single node means no high availability, stated plainly, and the missing `--tls-san` is named as the single most common failure mode. Rejects SSH-from-the-laptop as the spine (a permanent SSH-as-root, host-key, and key-handling surface bought for one fewer manual step; possible later as an optional front end over the same bootstrap), depending on k3sup, keeping the admin credential off the laptop (forces SSH for every governance op), and requiring a managed cluster or a Terraform provisioner | Accepted |
| [0045](0045-oss-enterprise-boundary.md) | The OSS/enterprise boundary — a single-tenant core that a private module extends — pins an existing declaration to the specific **seams** that let a private module extend this core without forking, because as features accrete it is easy to break one by accident (hardcode identity at a call site, bury the control-plane client behind a concrete transport) and only discover it later, when the extending module cannot cleanly wrap the core. This repo is a **single-tenant control-plane engine**; no auth provider, tenancy, org/team model, billing, or dashboard lives here, and the engine does not know about users or tenants. Three seams must be preserved: **(1) the module boundary** — `controlplane`/`operator` stay outside the top-level `internal/` precisely so a separate module can import their public API rather than fork it; **(2) the principal seam** ([ADR-0038](0038-scoped-agent-credential.md)) — a single substitution point where an authenticated identity is resolved, the constant `"shared-agent"` here, so the acting identity is never hardcoded at a call site; **(3) a pluggable control-plane transport and auth in the CLI** — self-host reaches burrowd over the kubeconfig API-server proxy, and an alternative token-based transport slots in behind an interface so **one CLI serves both**, rather than a second CLI reimplementing the commands. The record names **one refactor this repo owes**: make the CLI's control-plane client transport and auth an explicit interface, before more transport-coupled call sites accrete. `"shared-agent"` is framed as the degenerate single-user case of a general principal. **[ADR-0053](0053-in-cluster-build-from-source.md) §6's `Builder` seam, [ADR-0057](0057-source-provider-credentials.md)'s credential seam, and [ADR-0061](0061-deploy-pod-mutator-seam.md)'s pod mutator are the same model applied.** Rejects forking the core downstream (guarantees drift and double maintenance), building SSO and multi-tenancy into this engine behind flags, and a separate CLI that reimplements the commands | Accepted |
| [0046](0046-registry-onboarding.md) | Registry onboarding — auto-wire the code provider's registry first; an in-cluster registry for the no-cost self-hoster — dogfooding surfaced the "registry dance" (create an account, log in, provision a pull secret) as the sharpest-feeling step in the flow, which prompted an instinct to bundle a registry into the cluster. Two observations reshape that instinct. **Most agent-driven developers already host code on GitHub or GitLab**, both of which include a container registry, so the friction is not "there is no registry" but "wiring up credentials by hand" — plumbing Burrow can automate against a registry the user already has, bundling nothing. And **cost sensitivity splits the audience**: a developer willing to pay for a cloud load balancer is already comfortable with GHCR or GitLab, so an in-cluster registry is not a mainstream default but the zero-added-cost path for the single-VPS self-hoster. **Primary path:** given the code-host token the user already grants (with a package-write scope), Burrow targets the right registry namespace, configures `docker login` on the laptop, and mints the cluster pull secret — the honest cost being that a package-write scope is broad-ish, so the ask is opt-in and scoped as narrowly as the provider allows. **Second path:** run **zot** in-cluster and **publish it at `registry.<domain>`** through the ingress with a real Let's Encrypt certificate and auth, which keeps traffic at native line speed and **never transits the Kubernetes API server** (so a 1GB image does not pressure a small single node) and leaves nothing to distribute to the developer's Docker; an optional port-forward push to a `localhost` registry covers cold starts and small images only. **Held deliberately at Proposed**: the decision shape is recorded, but building waits on a signal that onboarding is actually painful — and the record notes that if the first path lands, most of the motivation for bundling evaporates. Rejects a self-signed NodePort registry (the laptop-side CA trust cannot be hidden without elevated privilege or disabling verification — dominated on both friction and security), control-plane-mediated push, a `type=LoadBalancer` registry, Harbor as the bundled engine (multi-component with its own Postgres and Redis, cannot fit a 2GB floor), and bundling as the primary answer | Proposed |
| [0047](0047-agent-environment-safety.md) | Agent environment safety — explicit, sticky environment targeting — the environment selector's own contract said the agent "targets explicitly and never rides the ambient context," yet the empty-argument path did exactly that, routing to whatever kube context happened to be current. The concrete failure: a developer running `prod` (their website) and a throwaway VPS shut the VPS down, then asked an agent to ship a release; the agent called the deploy and status tools with no environment, routed to the now-dead VPS's scoped credential, and **retried the same dead target** rather than consulting the environments list and discovering `prod` existed and was reachable — two compounding failure modes, an implicit target and the temptation to wander. Guidance was the immediate mitigation, but guidance protects only a compliant agent. Decision, enforced where the routing lives: **a mutating operation that names no target is refused whenever more than one environment is *registered*** — judged by registration, a static fact resolvable without a network probe — **at both axes** (the local handles and burrowd's own registry, so registering even one named environment beside the implicit `default` makes a bare mutation name its target), with a structured, alternatives-listing refusal the agent reads and re-issues. **A single registered environment proceeds without ceremony**, so the common self-hoster sees no change. **Read-only survey stays frictionless but every result echoes the environment it read**, so a survey never silently conflates two. And **the target is sticky**: an unreachable or errored target names the other environments so a human can redirect, but **the operation stops there — nothing auto-switches**, because a transient error on prod must never become a deploy elsewhere. Rejects having the agent inherit the human's CLI pin (the same implicit-target problem in another form, and the pin also pointed at the dead VPS), auto-failover to a reachable environment, guidance alone, requiring an explicit environment even when only one exists, and modelling this as a confirm guardrail (a missing target is an unanswered question, not a held operation) | Accepted |
| [0048](0048-one-off-command-runner.md) | One-off command runner (`burrow_run`) — an agent-invoked, synchronous, guarded runner for the tasks a long-running process cannot cover: database migrations, seed and fixture loads, backfills, a maintenance script, a REPL-style diagnostic. Without it the agent's only recourse is out-of-band `kubectl exec`, which routes around the control plane, holds no guardrail, leaves no audit record, and defeats the scoped-credential boundary that makes an agent safe to point at prod. It runs the caller's command **from the app's own currently-deployed image**, resolved through the same lookup deploy and rollback use, in the app's namespace with the app's config and per-app-Secret **`envFrom` injection**, so `DATABASE_URL` and every secret resolve exactly as the running app sees them — the whole value being the exact runtime, dependencies, and environment the app ships. It is **synchronous** (launch, wait, capture stdout/stderr and the exit code, return a structured result in which a non-zero exit is a normal outcome, not a transport error) with a 10-minute bound and **no streaming**, because Burrow has no streaming primitive and a one-off command does not need one. The **explicit call is the spine**, mirroring [ADR-0007](0007-explicit-deploy-by-image-reference.md): it is what lets the agent sequence and react — run then deploy, or express expand/contract as run → deploy → run — which a pre-deploy hook structurally cannot, and it can stop on failure. Gated by a new per-environment **`app.run`** guardrail (default `confirm`), audited, and reusing the [ADR-0032](0032-postgres-backups.md) Job machinery, with the one genuinely new capability being capturing a finished Job's output and exit code. Cleanup is `ttlSecondsAfterFinished` (default 1 hour, a per-invocation `ttl`, `ttl=0` allowed) applying to **both** terminal states — safe here, unlike backups, precisely because the result is captured before cleanup. **Honest limitation, stated as its own section: `app.run` gates the operation, not the command's contents** — a migration run through it may still `DROP TABLE`, and safety comes from environment-scoped gating, confirm-with-the-command-in-plain-view, and backups to recover, not from inspection. Rejects a Postgres-only `burrow_run_sql` (the only option that could do statement-level guardrails, but it needs a SQL parser and reinvents migration versioning and rollback, and covers only the database case — deferred and named, not discarded), a Heroku-style auto-run release hook, streaming, and an arbitrary-image runner | Accepted |
| [0049](0049-burrow-agent-scoped-cli-control-channel.md) | `burrow-agent` — a scoped CLI as the agent's control channel — retire the MCP server as the agent's primary interface in favour of a dedicated, capability-reduced, JSON-first binary the agent invokes directly. **The primary win is composability**: the agent can pipe, `grep`, `jq`, page, and chain `--json` output, which a fixed tool-call result blob structurally cannot offer — composition is the native idiom of a coding agent's environment (a shell). Three further costs it removes: a second thing to install and wire up, a persistent perception that a resident tool surface pollutes context, and the standing **parity tax** of building, documenting, and testing every operate-verb twice across two surfaces that can silently drift. **Two distinct binaries, not a subcommand carve-out**: `burrow` is the human admin CLI, while `burrow-agent` *intrinsically lacks* the dangerous verbs — no `install`, no `bootstrap`, no `cluster` setup, no `guard set`, no credential writes are compiled into it. The reason for two names rather than `burrow agent …` is portability: agent runtimes match permissions on **the executable** and let a deny take precedence over an allow, so allowing a subcommand while denying its parent is fragile where it works at all and is not portable across runtimes. Safety rests on **three independent layers** — the binary lacks the verb (structural, not a runtime check), the scoped token's RBAC lacks the permission, and the guardrails gate the rest server-side — which is strictly stronger than a deny list a runtime must honour at call time. Discovery moves from tool schemas to rich `--help` plus an instructions surface, and `burrow agent <tool> install` (a human-run subcommand of the trusted admin CLI, which is explicitly *not* the portability case) writes the permission rules. **Supersedes [ADR-0005](0005-mcp-server-holds-no-cluster-credentials.md)** — the credential boundary migrates unchanged — and explicitly does **not** supersede ADR-0004, ADR-0003, or ADR-0002. Rejects keeping MCP primary, maintaining both surfaces at parity, a subcommand carve-out as the runtime channel, and giving the agent the full binary behind a deny list | Accepted (supersedes ADR-0005) |
| [0050](0050-app-runtime-api-and-capability-envelopes.md) | App-runtime API and capability envelopes (a north-star direction, explicitly deferred) — expose a programmatic control-plane API that a **deployed application itself** can call at runtime to provision infrastructure for *its own* users: an ephemeral sandbox pod behind a preview subdomain, a bounded worker or job. The through-line is that **agent, human, and app become three clients of one enforced control plane** — the boundary does not move, the app calls burrowd, and burrowd remains the only layer talking to Kubernetes and the only place the grant is enforced. What is new is the enforcement mode: **interactive human confirmation does not transfer to runtime volume**, so instead of per-call approval an operator grants a **capability envelope once** — the namespace the app may provision into, a per-workload and aggregate resource ceiling, a maximum concurrent count, a subdomain pattern previews must fall under, and quotas and rate limits — which the control plane then **enforces continuously on every call**, refusing anything outside it. The natural first increment is narrow and already half-built: an app triggering a bounded job within its envelope is the [ADR-0048](0048-one-off-command-runner.md) one-off Job machinery **with a different caller**; full multi-tenant sandbox hosting is the destination reached incrementally, not jumped to. Strategically it is **land-and-expand within the same solo-developer user** rather than a new buyer — the developer grows *on* the platform instead of leaving for a separate sandbox tool, and this version is a platform primitive on durable, guardrailed Kubernetes rather than a disposable local sandbox. Two costs are named rather than solved: a **new scoped credential type** bound to an envelope and revocable, and **multi-tenant workload isolation** as the hard, unsolved part (NetworkPolicy, quotas, pod security, noisy-neighbour containment, crash and leak reconciliation). Rejects telling the developer to adopt a separate sandbox tool (loses both the stickiness and the enforced boundary), routing runtime provisioning through the agent (a category error at machine speed and frequency), and per-call human confirmation | Accepted |
| [0051](0051-postgres-always-exports-metrics.md) | Postgres always exports metrics; the scraper discovers the add-on namespace — a database is the most common reason a "why is my app slow?" investigation ends somewhere other than the app code (a missing index, a lock, a slow query), yet the metrics add-on discovered only app pods and nothing exported or scraped the shared Postgres running in the separate add-on namespace. Two forces shape the fix. **Install order must not matter** — a user may add Postgres first and metrics later or the reverse, and metrics discovery is dynamic (the Kubernetes API, re-read continuously), so annotating the pod and watching the namespace makes samples flow whichever landed first, where a one-time wire-up at install would be order-sensitive and would miss the common add-metrics-after-the-fact case. **The exporter is cheap; the store is not** — a sidecar costs a few MiB and produces metrics nobody stores until a backend exists, while storing and retaining them is the real cost, so the exporter is always-on and the store stays a deliberate opt-in. Decision: every Postgres add-on pod runs a pinned `postgres_exporter` sidecar on `:9187` (32Mi request / 64Mi limit), connecting over pod loopback with its password sourced by `secretKeyRef` and never inlined, and carrying the standard `prometheus.io/*` scrape annotations app pods already use. For `pg_stat_statements` data to exist, Postgres runs with `shared_preload_libraries=pg_stat_statements` and the extension is created on first boot by an init script — **created in the `postgres` maintenance database by pinning `POSTGRES_DB=postgres`**, because the image would otherwise default the database to the user name and strand the extension where nothing reads it. vmagent's discovery adds the add-on namespace, deduped when the two namespaces coincide, and its RBAC gains a matching namespaced Role pair (emitting only one when they are the same, since two identically-named Roles in one namespace would collide on apply). **Known limit: the add-on install path is create-only, so an existing Postgres add-on gains the exporter only on reinstall.** Rejects wiring scraping at attach or install time, making the exporter opt-in, a separate exporter Deployment rather than a sidecar, and granting vmagent cluster-wide pod discovery | Accepted |
| [0052](0052-pull-based-passive-deploy.md) | Pull-based passive deploy — a semver-scoped registry watcher on the guarded path — the concrete specification of the optional passive mode [ADR-0007](0007-explicit-deploy-by-image-reference.md) allowed but refused to make the spine. Two facts force the shape. **Push-from-CI cannot serve a private cluster**: burrowd has no public endpoint of its own and is reached through the Kubernetes API server's proxy, so an external runner must reach the *API server* — which a managed cloud exposes but a VPS or home cluster firewalls, NATs, or deliberately does not expose, making push a dead end for exactly the target user. **A pull model needs only outbound connectivity, which every cluster has.** So burrowd polls the registry's tag list and, when a new tag is within the app's per-environment level, invokes **the same internal guarded deploy an explicit call uses** — same guardrails, same rollout, same `Supersedes` chain (the rollback handle), same audit entry — indistinguishable downstream except in recorded provenance, and adding **no inbound surface**. The **level** (`off`, `patch`, `minor`, `major`) is the control a mutable-tag model lacks, and every level moves **upgrades only**, to the highest semver within the cap that exceeds the running release, **by version order and never by push time**, so the watcher can only move an app forward. A tag above the cap is **surfaced as an available upgrade with the exact command**, never silently applied — *held, and you are told*. **A rollback or a manual downgrade sets the level to `off`, with the reason surfaced**, so the watcher cannot fight a deliberate rollback minutes later. Polling is conservative (about 5 minutes, jittered, authenticated, using conditional requests and backing off on `429`) precisely because **the explicit deploy is always the immediate path**. Non-semver tags are ignored rather than chased, and Burrow **guides the agent toward semver without enforcing it** (suggest the next tag, hint on a non-semver deploy). **§2 and §5's on-by-default `minor` are superseded by [ADR-0058](0058-auto-deploy-is-opt-in.md)**; the rest stands. Rejects mutable-tag auto-pull, push-from-CI as primary, forbidding major auto-updates entirely, and making passive the spine | Accepted |
| [0053](0053-in-cluster-build-from-source.md) | In-cluster build from a source reference (optional, off the explicit spine) — Burrow stays **client-build-first**, and this adds an optional path for users who do not want a CI dependency: the control plane clones a git ref and builds the image **inside the user's own cluster** as a Kubernetes Job, removing both frictions the client path carries (wiring a CI system to turn a push into an image, and creating an external registry account plus pull tokens). Five properties keep it off the spine and inside the invariants. The trigger is an **explicit call** (`burrow app build <app> --source <git-ref>` plus an agent verb), and the control plane reaches **outward**, needing no inbound or public endpoint. **Code never crosses the control channel — only the git ref does**; the builder clones the source in-cluster, so no tarball, diff, or source byte is ever carried. The build runs as a Job using **Buildpacks for the no-Dockerfile case and buildah when a Dockerfile is present** (Kaniko is archived upstream and not used; BuildKit is the caching alternative), and **the resulting image reference flows into the existing guarded deploy path** — build is a front-end that ends where deploy begins and produces nothing downstream can tell apart except in provenance. An **optional in-cluster registry (Zot, with the CNCF `distribution` registry as the heavier alternative)** is the zero-config default push target, with external registries fully supported. And **the builder sits behind a deliberately minimal `Builder` seam** — source ref in, image digest or error out — with isolation expressed *inside* an implementation rather than as interface knobs, so a hardened sandboxed executor can be supplied without the interface anticipating its needs. §7 states the trust model plainly: this path is **single-tenant and the user owns the source**, so build-time code execution is a risk only to themselves and the restricted PodSecurity floor is defense in depth, not an adversary boundary. **§7's floor is refined by [ADR-0056](0056-build-security-context-for-the-oss-builder.md) and then, after live validation, superseded by [ADR-0059](0059-oss-build-container-runs-privileged.md)**, which runs the build container privileged; §6's seam and the trust argument carry forward unchanged. Passive build-on-push reuses the outbound-only poll shape of [ADR-0052](0052-pull-based-passive-deploy.md), with a webhook available only on the advanced public-HTTPS path. Rejects build over the control channel, external-registry-only, webhook-as-spine, build-as-spine, and a heavyweight in-cluster CI system | Accepted (§7's build-container security context refined by ADR-0056) |
| [0054](0054-install-is-control-plane-only.md) | `install` provisions only the control plane; additive components are standalone `cluster` commands — convenience flags (`--with-registry`, `--with-ingress`) had crept onto install and bootstrap, and they are a design mistake worth correcting before more accrete: they make "what does install do?" depend on which flags were passed, they hide a component behind a flag on an unrelated command where it is undiscoverable and un-removable, and they scale badly, since every future component would want its own flag on both commands. Decision: **install and bootstrap provision the control plane plus a lightweight, detected baseline, and nothing else**, and every additive *capability* becomes a **standalone `burrow cluster <component>` command** with a uniform shape — the bare command reports status, `install` provisions, `uninstall` reverses. The **baseline carve-out** is deliberately narrow, admitting a component only if it is **lightweight**, a **recognized baseline that some vendors already ship** (so its absence is vendor inconsistency rather than a deliberate user choice), and **load-bearing for cross-cluster parity**; **metrics-server is the first**, because it powers autoscale and capacity reporting and vendors ship it inconsistently, so without auto-ensuring it, autoscale silently degrades on some clusters and not others. Billable or storage-hungry components fail the test and stay opt-in. The ADR adds `burrow cluster registry` and pins that registry as **one cluster-agnostic mechanism**: Zot behind the cluster ingress with the existing HTTP-01 issuer, the build **pushing to an internal ClusterIP Service over plain HTTP** while nodes **pull through the ingress over TLS** with a generated credential — so **no node or containerd editing**, identical on k3s and a managed cluster, at the cost of requiring a hostname (a documented no-domain limitation, alongside a buildpacks insecure-push gap). A component that depends on another verifies it and fails with the command that provides it, rather than half-installing. Rejects the `--with-*` flags, the earlier NodePort-plus-`registries.yaml` design (k3s-specific, requires running the install on the node, splits behaviour by cluster type), a generic `addons` umbrella, and folding privileged provisioning into the guarded API | Accepted |
| [0055](0055-multi-version-upgrades.md) | Multi-version forward upgrades in one step, guarded by a version floor — [ADR-0013](0013-database-migrations-and-upgrade-policy.md) refused any jump wider than one minor and gave two reasons, and this record meets both head-on. Reason (a), bounding the tested surface, **does not hold for the migrations Burrow ships**: they are a **linear chain**, not independent N→M paths, applied by goose in numeric order whether in one call or across several upgrades, so the tested surface — each migration on top of its immediate predecessor — is **identical whether stepped or jumped**. The only thing single-stepping adds is running the intermediate *binary*, which matters solely for a migration depending on application code running out of band; an audit of the shipped migrations finds none (all pure forward-only SQL, no Go migrations anywhere in the tree, and the three that transform data do it in-migration). Reason (b), safe squashing, **is real but does not require single-step**: it needs a **version floor** at each squash boundary, which is a narrower instrument. Decision: apply **all** pending migrations in one startup call under the existing advisory lock, and shrink the gate to **three refusals — a downgrade, a cross-major in-place move, and a start below a defined floor**; an ordinary multi-minor forward jump within a major is now allowed. This takes on a new standing invariant, **§3: every migration is self-contained** — any transformation happens inside its own SQL, and a change that genuinely needs a program to run mid-chain is not an ordinary migration but an explicit floor boundary. Because [ADR-0039](0039-cli-control-plane-version-skew.md)'s one-minor compatibility band was justified *by* the single-step rule, **it is widened here in the same breath** to any same-major client no newer than the server and at or above the floor. §6 demotes version cadence to **guidance, never a safety mechanism**. Rejects keeping single-minor-step, dropping the gate entirely, abandoning squashing, widening one rule while freezing the other (incoherent), and relabelling feature releases as patches to sidestep the guard — pointedly, because that would make a human labelling mistake a data-correctness bug | Proposed |
| [0056](0056-build-security-context-for-the-oss-builder.md) | The build container's security context — relax restricted PodSecurity for the rootless builder — [ADR-0053](0053-in-cluster-build-from-source.md) §7's floor (non-root, RuntimeDefault seccomp, `allowPrivilegeEscalation: false`, all capabilities dropped, read-only root) cannot run a rootless container builder, and the reason is structural: **building a container image inside a container fundamentally requires either root-in-container or a user namespace**, and the one builder that avoided that (Kaniko) is archived upstream. The floor blocked buildah on two fronts — RuntimeDefault permits the namespace-creating `unshare(CLONE_NEWUSER)` only with `CAP_SYS_ADMIN`, so with all capabilities dropped it is refused outright, and `allowPrivilegeEscalation: false` sets `no_new_privs`, which neuters the setuid `newuidmap`/`newgidmap` helpers rootless buildah uses to write its id maps. So the question was never "find a builder that runs under §7" but "how much to relax, and on what grounds." This ADR relaxes by **exactly the set the rootless builder needs and no more** — recommended as `seccompProfile: Unconfined` plus `SETUID`/`SETGID` and `allowPrivilegeEscalation: true`, preferring unconfined seccomp over `CAP_SYS_ADMIN` because the former opens only syscalls while the latter is the single broadest capability in Linux — while **keeping** the non-root UID, the read-only root filesystem, every other capability dropped, and the resource caps, and leaving the clone init container at the full floor. §2 supplies the grounds: this path is **single-tenant and the user owns the source**, so §7 was defense in depth, not an adversary boundary; untrusted-source isolation stays behind the §6 `Builder` seam in a sandboxed executor, never in PodSecurity. It also insisted the exact set be **validated on a real cluster**, expecting a round or two of iteration. **That validation superseded this record: [ADR-0059](0059-oss-build-container-runs-privileged.md) found the narrow set clears the `unshare` wall but not buildah's remount of the locked container root, and moves to a privileged build container** — carrying §2's trust argument and §3's seam forward unchanged. Rejects keeping §7 as-is (the build simply cannot run), switching builders (BuildKit-rootless hits the identical wall), root-in-container, and `CAP_SYS_ADMIN` over seccomp Unconfined | Superseded by [ADR-0059](0059-oss-build-container-runs-privileged.md) |
| [0057](0057-source-provider-credentials.md) | Source-provider credentials — one provider token for private-git clone and its registry — the in-cluster build clones with **no credential at all**, so it can only clone public repositories, and a private `--source` dies at the fetch with `could not read Username` (git falling back to an interactive prompt that has no TTY in a Job) — the same message a *nonexistent* repo produces, because hosts return auth-required rather than leak whether a private repo exists. That excludes the common case: a solo developer's app lives in a private repo. Decision: a **source-provider credential keyed by provider** (`github`, `gitlab`, …), each holding **one token**, referenced by **both** consumers that authenticate to that provider — the build clone for private git, and, where the same provider hosts a registry, the image pull as registry auth. The unit is the provider because that is exactly the span over which one token is valid for both: a GitHub PAT covers `github.com` clones **and** `ghcr.io`, and a GitLab token covers `gitlab.com` and its registry, so a second, separate token for the clone would be setup with no security gain. The credential is a **seam** — "give me a usable token for provider X, repo Y" — with **a stored PAT for self-host and installation tokens for the hosted case behind the same interface**, because an App requires an inbound OAuth callback a private, NAT'd cluster lacks (the same reachability wall that made [ADR-0052](0052-pull-based-passive-deploy.md) and [ADR-0053](0053-in-cluster-build-from-source.md) poll) and either centralizes trust or adds more setup than a PAT. Transport is the guarded path: set through the control plane, written to a Secret, never over the agent channel, never logged, never in Postgres — and **consumed by mounting, not passing**, via a git credential helper or `url.insteadOf`, so the token never appears as an argument, a Job env var visible in the spec, or a command line. §5 keeps sharing **allowed, not forced** (non-provider registries keep the per-host login), and §6 recommends a fine-grained PAT or a per-repo deploy key while naming one broad PAT as the pragmatic solo default and its blast radius honestly. Rejects an App for self-host, separate per-subsystem tokens, carrying the token as a tool argument, folding it into the host-keyed registry login (a registry host knows nothing about git), and a broad PAT as the only supported form | Accepted |
| [0058](0058-auto-deploy-is-opt-in.md) | Auto-deploy is opt-in — the default level is `off` — revises [ADR-0052](0052-pull-based-passive-deploy.md) §2 and §5, which made `minor` the default and turned auto-deploy **on for every app, new or already deployed**. That has a sharp edge on upgrade: when a cluster moves to a poller-carrying version, a **pre-existing app that never opted in immediately begins being polled**, and because the poller lists the registry anonymously with no read credential of its own, a private repository answers `401` and the watcher logs it every interval — recurring noise for an app the operator never asked to auto-deploy. The deeper problem the record names is not the log line but the surprise: **installing or upgrading Burrow silently changed what may deploy to an app**, and "what deploys unattended" is precisely the class of decision Burrow keeps in human hands — the same reason the agent cannot set the level. The on-by-default choice also contradicted ADR-0052's own title. Decision: **an app with no stored level row reads `off`**, so the watcher's per-app reconcile reads the level first and **returns before contacting the registry** — an app that never opted in is never polled, no tag listing, no `401`, and the noise is removed at its source rather than suppressed. Turning it on is a deliberate `burrow app auto-deploy <app>` with a level, per environment; until then only the explicit guarded deploy ships a release. **Only the default changes** — level semantics, the guarded path, the surfaced above-cap upgrade, the rollback safety stop, and the conservative cadence are all unchanged — and the release-notes migration ADR-0052 anticipated is no longer needed. The poller's read-credential story stays open and is named, with its listing failure now logged once per distinct error rather than every interval. Rejects keeping on-by-default, defaulting off for pre-existing apps and on for new ones (a hidden, time-dependent rule resting on state the operator cannot see), and suppressing the poller's registry errors instead of fixing the default | Accepted |
| [0059](0059-oss-build-container-runs-privileged.md) | The build container runs privileged — **supersedes [ADR-0056](0056-build-security-context-for-the-oss-builder.md)'s mechanism**, which ADR-0056 itself asked to be pinned on a real cluster. Validated end to end on a live managed cluster (DOKS/containerd), the narrow set is **necessary but not sufficient**: it clears the `unshare(CLONE_NEWUSER)` wall it targeted, then fails on the next one — buildah's layer extraction (`chrootarchive`) remounts the container **root mount** private before pivoting, and a managed CRI creates that mount **locked**, so changing its propagation is refused. Isolating the cause in a throwaway pod mirroring the build context settled it conclusively: a **writable** root filesystem changes nothing (the block is the mount *lock*, not the read-only flag); **`CAP_SYS_ADMIN` changes nothing** (a namespaced `CAP_SYS_ADMIN` cannot change propagation on a mount the parent namespace locked, reproducible with plain `unshare -Urm` outside buildah entirely); and **`privileged: true` completes the build**. ADR-0056's narrowest-first ordering is sound in principle but **bottoms out here: on managed Kubernetes there is no rung below privileged that runs the build**. Decision: the build container runs `privileged: true`, `seccompProfile: Unconfined`, `allowPrivilegeEscalation: true`, and a **writable** root filesystem; the clone init container keeps the full floor. **ADR-0056's trust argument carries over unchanged and is what still makes this acceptable**, as does its seam-based isolation hook for the untrusted-source case, which never runs a privileged pod on a shared node. Because privileged is broad, **the bounding moves to namespace and lifetime**: a dedicated `burrow-builds` namespace isolated from both the app and control-plane namespaces (so a build cannot reach a running app's Secrets or burrowd's credentials and database), a non-root UID, resource caps, and a transient TTL-reaped Job — with the Pod Security Admission `privileged` label scoped to that one namespace. The adapter also pins buildah's storage explicitly (a private vfs graphroot and runroot under `$HOME` at `0700`, a private `XDG_RUNTIME_DIR`, a writable `TMPDIR`), because the default falls back to a group-writable path buildah refuses. Rejects ADR-0056's set, `CAP_SYS_ADMIN`, and writable-root-alone (all validated insufficient), and defers `hostUsers: false` user namespaces until dependable across providers | Accepted |
| [0060](0060-cluster-lifecycle-command-group.md) | `cluster` is the cluster-lifecycle command group; operate verbs stay portable — an inconsistency had opened up: `bootstrap` is install-plus-baseline and lived under `burrow cluster` alongside `ingress`, `registry`, and `capacity`, while plain `install` and `upgrade` sat at the top level next to the operate verbs, so two commands doing nearly the same job lived at different altitudes. The deeper line the record draws is **self-hosted-only versus portable**: installing or upgrading the control plane, provisioning ingress, and reading raw cluster capacity all **drive a kube context**, which only a self-hoster has, whereas the operate verbs, `env`, `agent`, `config`, `guard`, and `audit` speak to a control plane **over its API** — and that control plane can equally be one the user self-hosts or one run on their behalf, reached generically with the same client given an endpoint URL and a token ([ADR-0045](0045-oss-enterprise-boundary.md)). Decision: `install` and `upgrade` move to **`burrow cluster install` / `burrow cluster upgrade`**, joining the existing cluster commands, with bare `burrow cluster` keeping its read-only capability report and the getting-started guidance and first-run banner repointed. The portable commands stay top level, which means **the top-level surface is now exactly the portable one**, so **the CLI needs no mode switch and its command surface reveals nothing about how it is being reached**. The old spellings remain as **deprecated, hidden top-level aliases** that delegate to the same command constructor and print a one-line migration hint, so scripts and muscle memory keep working through the transition; Cobra excludes deprecated commands from the main help, so they execute without cluttering it. Rejects leaving `install`/`upgrade` at the top level (keeps the `bootstrap` inconsistency and mixes self-host-only commands into the portable surface), removing the old spellings outright (a needless courtesy break even pre-1.0), and adding a mode flag or a mode-specific command group | Accepted |
| [0061](0061-deploy-pod-mutator-seam.md) | A pod-mutator seam on the deploy path, mirroring the build one — `Adapter.WithPodMutator(func(*corev1.PodSpec))`, applied to the Deployment's pod template **after it is constructed and before the object reaches the API server**, with deliberately the same signature, the same nil-means-unchanged default, and the same naming as [ADR-0053](0053-in-cluster-build-from-source.md) §6's `WithBuildPodMutator`, so a reader who understands one understands the other and there is no second concept to learn. The problem is that the app pod is built as a **fixed literal** nothing outside the package can adjust, and **that shape is not universally deployable**: a cluster whose only schedulable capacity is **tainted** (a GPU pool, spot capacity, a pool held for one team) admits a pod only if it carries the matching **toleration**, and Burrow's app pods carry none — so the deploy stays `Pending` forever with no way for the operator to fix it. The same applies to a mandated `runtimeClassName`, a priority class, topology constraints, a `nodeSelector`, or a pull secret for a private base registry. None of these belong in the engine — they are properties of *a* cluster, not of Burrow — but a fixed literal makes them **impossible rather than merely unspecified**, and the asymmetry is the anomaly: the same operator can already adjust the **build** pod and not the app pod, and the app pod is the one that runs indefinitely. **§2 applies the mutator on update as well as create**, because a create-only hook is silently dropped by the first rollout — a regression that surfaces later, under load, presenting as a scheduling problem rather than a missing hook. §3 makes "a nil mutator leaves current output byte-for-byte unchanged" a **test obligation**. The consequences are named honestly: the hook is trusted, in-process, and unvalidated; a non-idempotent mutator will drift because it runs on updates (appending to a slice is the obvious trap); and two installs on the same version can now produce different pods, which makes the mutator the first thing to inspect when they differ. Rejects forking the deploy path downstream (drift in the most churn-prone code), **patching the Deployment after creation — a race, since pods can start before the patch lands, so where the missing field is a sandboxed runtime the window is a workload running unsandboxed** — a mutating admission webhook (another component, its own certificate lifecycle, a down-webhook failure mode, imposed on installs needing none of it), per-field configuration on the engine (a list with no natural end), and a whole-object mutator (wider than the need) | Accepted |
| [0063](0063-object-storage-provider.md) | Object storage is a provider type, scoped to being a backup destination — [ADR-0032](0032-postgres-backups.md)'s dumps land on a PersistentVolumeClaim in the **same cluster as the database**, so they share its failure domain: they survive a bad migration but not the loss of the cluster or the volume, and the missing schedule and retention both need the same thing first — a destination that is not this cluster. Object storage becomes a provider *type* on [ADR-0023](0023-provider-credentials.md)'s existing registry: same `burrow-credentials` Secret, same `providers` table, same `resourceNames`-restricted `get`, **no new credential mechanism and no new RBAC**. Addressed by **S3-compatible endpoint rather than by vendor**, so the vendor is a configuration value and not part of the architecture. **The substance of the record is the SCOPE, not the feature.** Every vendor ships a capable object-storage CLI and one of them is enormous; Burrow must not become a worse one, and the record enumerates what it deliberately does not do (no `cp`/`sync`/`ls` of arbitrary prefixes, no presigned URLs, no bucket policy/IAM/replication surface) so that widening it is an amendment rather than a drift — a capability enters only when a Burrow feature requires it, never because the API offers it. What Burrow owns is the seam those CLIs cannot see: putting the credential where the backup engine will look for it (a Secret, in a namespace, in a shape, named by a CR field — which is where the time and the mistakes go); **verifying the destination at configuration time** by writing and deleting a probe object, so a wrong key fails now and loudly rather than at the first scheduled backup, silently; and §3's load-bearing invariant, **reconciling bucket lifecycle against backup retention** — a lifecycle rule that expires objects sooner than the oldest backup needs them leaves a backup set that lists fine and cannot be restored, and neither the vendor's CLI (which knows nothing about backups) nor the backup engine (which does not own the bucket policy) can see both halves. Where Burrow cannot read a bucket's lifecycle configuration it says so rather than implying it checked, because an unverifiable invariant reported as verified is worse than one reported as unknown. §4 guardrails bucket creation and deletion, deletion **denied by default** (its blast radius is every backup the platform holds). §5 makes one registration serve every consumer, and permits several providers, since the registry is a table rather than a singleton. Notes the wrinkle that an S3 credential is a **pair** where ADR-0023's Secret holds one opaque token per provider. Leaves three sub-decisions explicitly open: how that pair is stored (settle before the first provider ships — changing it later is a credential rotation for every user), whether bucket *creation* belongs at all or only bucket *use*, and what happens when the store is unreachable at backup time. Rejects telling users to run the vendor's CLI (does not wire the credential, reconcile lifecycle, or make the operation agent-safe), a full object-storage command surface (a second-rate S3 client with no natural end), supporting one vendor properly (makes the vendor a dependency of the architecture), keeping backups on a PVC with volume snapshots (same failure domain; snapshot retention is the operator's problem), and a dedicated Secret separate from `burrow-credentials` (forks ADR-0023's model for one provider type; scope the credential at the vendor instead) | Accepted |
| [0064](0064-addon-removal-keeps-its-data.md) | Removing an add-on keeps its data; destroying the volume is a separate, operator-only act — **supersedes [ADR-0031](0031-postgres-addon.md)'s Teardown section**, which deleted the PVC on removal. For the Postgres add-on that is EVERY attached app's database, and the confirmation said only `removing the add-on "burrow-postgres"` — gated, but not informed. Two aggravating facts: remove-and-reinstall is the obvious REPAIR move, so the recovery-shaped use of the verb was its most destructive one; and the blast radius is not the add-on's own, since apps whose data lives in that volume did not participate in the decision. Removal now tears down the Deployment/Service/config and **leaves the claim**, so a reinstall picks the data back up and attached apps reconnect on their existing `DATABASE_URL` (role passwords live in PGDATA). Destroying it needs `--delete-data`, **absent from the agent binary** rather than denied — a disposition is a row someone can change ([ADR-0065](0065-what-belongs-on-the-agent-surface.md) tier 1) — plus, on a terminal, **typing the add-on's name** after a warning enumerating the volume and the affected apps, refusing outright off a terminal unless told otherwise. The prompt is explicitly **not** a security control: anything with a shell can type a word, stated so the structural absence is never relaxed on the grounds that a confirmation exists. Enumeration is best-effort and never blocking, because an add-on is often removed precisely BECAUSE it is wedged and an unremovable broken add-on is the worse failure. §4 keeps the backup claim always, including under `--delete-data` — backups outliving their source is the point, and it is what makes the destructive path survivable. §5 takes a final backup FIRST where object storage is configured (ADR-0063), aborting if it fails, so nothing is destroyed until a copy is known to exist; it cannot be mandatory, since a wedged instance cannot be dumped and requiring it would trade data loss for an undeletable cluster. §6 lists retained volumes, which is what makes keep-by-default defensible — a bill is a worse way to learn about a volume than a listing — and no automatic reaper is added, since a timer that deletes the data this record protects reintroduces the failure by a slower route. Records two discovered facts: the backup claim ALREADY survived removal (accidental and correct), and keeping the volume while regenerating the superuser Secret would be a trap, because the official image honours `POSTGRES_PASSWORD` only during `initdb` — a database present and permanently unauthenticable — so Secret and volume share a fate. Leaves open whether `--delete-data` deserves its own guardrail code, and whether the listing should report cost or only size. Rejects keeping the delete and improving the prompt (a poor defence for an irreversible act, and the users most likely to lose data are the ones debugging), hard-refusing while apps are attached (makes a wedged add-on unremovable when removal is the repair), putting the flag on the agent surface behind a deny (a row versus an absent verb), and deleting the backup volume too (removes the only path back from the most destructive flag in the product) | Accepted |
| [0065](0065-what-belongs-on-the-agent-surface.md) | What belongs on the agent surface — three tiers, by blast radius and reversibility. **Refines [ADR-0049](0049-burrow-agent-scoped-cli-control-channel.md)**, which established that the agent gets its own narrow CLI but never said what QUALIFIES a command to be on it, so each addition was argued from scratch and the surface drifted. The pattern is otherwise clean — `attach` without `detach`, `backup` without `restore` — with one exception: `addon remove`, the single most destructive verb in the product, because add-ons are **one per type per cluster** (`InstallAddon` takes a *type*; `addons.name` is its primary key) and ADR-0031 puts every app's database on that one shared Postgres. It removes THE add-on, not AN add-on, and there is no configuration in which its blast radius is small. Two questions decide placement: does the effect reach beyond the app the agent was asked about (**scope**), and can a human restore the prior state (**reversibility**). Failing scope is disqualifying — tier 1, **absent from the binary**, asserted by the surface-guard test. Failing only reversibility means the operator decides — tier 2, **denied by default** as a guardrail disposition. Routine-but-consequential stays tier 3, **confirm** (`app.run`, which is ADR-0048's whole purpose). Changes: `addon remove` leaves the binary; `app.delete` and `dns.delete` move `confirm` → `deny`. **Tier 2 is preferred to tier 1 wherever risk allows**, and the reason is behavioural rather than aesthetic: a denied verb is legible and anticipatable through the read-only `guard` command, while an absent one yields `unknown command` — a dead end that invites an agent to route around the control channel entirely, the failure burrow ADR-0021 says Burrow cannot close from the inside. §7 therefore has `guard` report ABSENT capabilities too, so an agent can tell a human "this is not something I can do, and here is who can"; it enumerates the surface, accepted because `--help` already does. A tier-2 default is a **floor, not a fixed setting**: `app.*` codes are environment-scopable, so the expected shape is a gradient — allow in dev, confirm in staging, deny in prod. Names the limitation honestly: `EnvScopable` keys on the `app.` prefix, so `dns.delete`'s deny is **cluster-wide and all-or-nothing**, which argues for widening environment scoping beyond `app.` in a separate change. Load-bearing dependency, stated: the middle tier holds only while `guard set` stays off the agent surface — and `guard set` is currently **unaudited**. Accepts that the agent can create apps it cannot delete, so they accumulate until a human intervenes: the same trade as ADR-0064's retained volumes, since an unnecessary app costs money and a wrongly deleted one costs data. Rejects removing every destructive verb (forecloses legitimate operator choice and produces dead ends), leaving everything at confirm (a control only for someone who reads it, and these are the cases where the reader is mid-incident), a single "destructive" flag (collapses unbounded blast radius and mere irreversibility, which warrant different mechanisms), and per-command policy (a second policy surface beside guardrails) | Accepted |
| [0066](0066-postgres-on-cloudnativepg.md) | The Postgres add-on runs on CloudNativePG; Burrow creates custom resources, not backups — **supersedes [ADR-0031](0031-postgres-addon.md)'s implementation mechanism** (the single-replica Deployment, the image, the teardown [ADR-0064](0064-addon-removal-keeps-its-data.md) already revised) while keeping its user-facing contract: an instance shared by an environment's apps, a database and login role per app, scoped one-per-environment by [ADR-0067](0067-one-database-instance-per-environment.md). Ten gaps, and they are not independent: backups on a PVC in the SAME cluster as the database, no schedule, no retention (so the disk fills and then new backups fail too), no PITR, one replica with `Recreate` so every drain/restart/upgrade is downtime, no pooling, no major-version upgrade path, and metrics with nothing alerting on backup age. Closing them by hand means implementing WAL archiving, scheduling, retention and point-in-time recovery — reimplementing a backup tool and being wrong about it somewhere that only surfaces at restore. **The most valuable change is what LEAVES the code:** Burrow creates a `Backup` object and reads `.status`; the operator does the work, so the Job construction, the `pg_dump` argv, the Job-watching and the superuser credential on the backup path all go, and backup correctness stops being this project's to get wrong. On licensing, which decided the shape: ADR-0031 rejected an operator citing "a bundled CloudNativePG with copyleft tooling", but **CNPG itself is Apache-2.0 and a CNCF project** and clears ADR-0025's bar — what is GPL-3.0 is **barman**, the Python tooling the best-known plugin shells out to. This uses a **pgBackRest** plugin instead (Apache-2.0 plugin, **MIT** engine). The record states the licensing standard in a form that can actually be applied, since "no GPL bytes" forbids every Debian-derived image (glibc, libgcc) and `postgres:17-alpine` is neither Apache nor MIT: no copyleft compiled into or linked by Burrow, nothing whose license reaches Burrow's or a user's code, and prefer permissive engines where a real choice exists. The WAL-G plugin is the case proving IMAGES must be audited and not just repositories — Apache plugin, Apache engine, and a published image built with GPL-3.0+ lzo. **Writing our own plugin is rejected**: `archive` carries PostgreSQL's `archive_command` contract transitively, so returning success means the segment may be recycled and must therefore mean durably stored, the call must be idempotent because failure retries forever, and nothing tells a plugin which WAL segments the oldest base backup still needs. Records what CNPG does NOT do, which is what Burrow must own: `Backup.status` carries **no size field** (join `VolumeSnapshot.status.restoreSize`), retention **does not cover volume snapshots** and their default deletion policy retains them indefinitely, and recovery **always bootstraps a new `Cluster`** — never in place and **never per-app**, so the logical `addon restore <addon> <app>` path STAYS: physical recovery would rewind every app sharing the instance, a cross-app data loss triggered by a single-app operation. The two coexist deliberately (logical = one app's database at the moment of the dump; physical = the whole instance at any point in the WAL window) because a shared instance with a database per app has both failure modes and neither backup kind covers the other's. §5's backup-age signal comes from what Burrow observed, because CNPG's own backup status fields and metrics were deprecated in 1.26.1 and report **stale values rather than absent ones** under plugin backups, so an alert built on them fails silently. §6 makes migration explicit and one-way. Names the honest costs: cluster-scoped CRDs mean `addon install postgres` stops being namespaced (narrowing ADR-0034's demand-driven provisioning, since installing CRDs needs cluster-admin the agent must not have), the small self-hoster pays for an operator, the chosen plugins are **experimental** and unverified against the intended object storage — so adoption includes a real restore from the real destination first. Rejects keeping the Deployment and building the missing pieces, the barman-cloud plugin (licence only), the WAL-G plugin (GPL in the shipped image), writing a WAL plugin, a Postgres per app (ADR-0031's density argument, unchanged), and managed Postgres (orthogonal — Burrow can already be pointed at an external database) | Accepted |
| [0067](0067-one-database-instance-per-environment.md) | One database instance per environment, and the first environment is called `prod` — closes a **latent data-corruption bug** before the feature that triggers it ships. `EnsureAppDatabase(ctx, app)` takes no environment, and the database and role are named `<app>` / `app_<app>`, so once [ADR-0035](0035-environments.md) phase 2's namespace-per-environment is used, `web` in staging and `web` in production resolve to the same database owned by the same role on the same instance — and because provisioning is **idempotent** the second attach does not fail: it rotates the role password and hands staging a `DATABASE_URL` pointing at production's data. Nothing errors, nothing warns, and both apps look correctly configured from every angle Burrow can show. A collision that errors is a bug report; one that silently succeeds is a data incident found later by its consequences. §1 fixes it with **one add-on instance per environment**, deliberately choosing the more expensive of the two available fixes: environment-qualified names (`web_staging`) on one instance would also isolate the DATA completely and cost nothing extra, but they cannot isolate the SERVER — one volume, one `max_connections`, one autovacuum, so a staging table growing without bound stops production accepting writes and a staging load test exhausts the connection limit for everyone (production outages caused by the environment that exists to be where things break, not data leaks) — and, decisively, **a major-version upgrade cannot be rehearsed on a shared instance at all**, because an upgrade applies to the *server* (one binary, one data directory, one catalog version; `initdb` refuses a directory written by a different major version), so there is no arrangement where staging runs 18 while production stays on 17 and upgrading moves production in the same operation. Isolation therefore comes from the instance, not a naming convention, and the provisioning seam takes the environment non-optionally — a signature that can omit it is one that will. §2 has install create ONE environment named **`prod`**, refining ADR-0035 phase 2's implicit `default`: the decisive reason is guardrails rather than naming taste, since [ADR-0065](0065-what-belongs-on-the-agent-surface.md) makes `app.delete`/`dns.delete` deny-by-default and expects a per-environment gradient, and an environment called `default` invites `guard set --env default app.delete allow` as the obvious way to stop the friction — relaxing **production** without the operator ever typing the word. §3 maps `prod` to the existing app namespace rather than `burrow-apps-prod`, so installs predating this need no migration. §5 makes sharing one instance across environments **unsupported** rather than discouraged, because a shared instance is only safe with qualified names and carrying both isolation models is how this collision arose. Names the direction of error plainly: a genuinely sandbox cluster gets production-grade defaults and will find them strict, which is the right way to be wrong — over-strict on a sandbox is a `guard set` away, under-strict on real production is not recoverable. Rejects qualified names on a shared instance (fixes only the collision; correct fallback if the per-instance cost proves prohibitive, and to be reconsidered on those grounds), keeping the name `default` (makes the safe defaults inert), creating no environment at install, an instance per app (ADR-0031's density argument, unchanged), and deferring until someone creates a second environment (the trigger is a user action, so whoever runs `env add staging` first gets the incident) | Accepted |
| [0068](0068-operational-limits-are-configuration.md) | Operational limits are configuration, set by a human, scoped to an environment — `app.replica_ceiling` is a guardrail code whose disposition is settable, so `guard set app.replica_ceiling allow` makes the ceiling **advisory**: a limit you can turn off is not a limit. The bound itself is a hardcoded `MaxReplicas: 50` with no CLI, API or store path, so an operator can DISABLE the ceiling but cannot RAISE it — exactly backwards. The root is a conflation: a guardrail answers *what happens when this is attempted*, a ceiling answers *where the line is*; merged, the line can be dispositioned away and the disposition has no useful values (`confirm` on a bound is a limit with extra steps, `allow` is no limit). §1 gives configuration three tiers — app config/secrets (developer-owned, reaches the pod, [ADR-0028](0028-app-config-and-secrets.md), unchanged), **environment configuration**, and **cluster configuration**, the middle-versus-outer test being whether a sensible answer can differ per environment. §2 makes a limit a bound whose breach is a **validation failure, not a policy decision**, and removes `app.replica_ceiling` from the guardrail set, with stored dispositions **dropped by migration** rather than silently ignored — an unrecognized code left in the table is a setting an operator believes is in force. §4 puts setting a limit on the operator CLI only and **absent from `burrow-agent`** ([ADR-0065](0065-what-belongs-on-the-agent-surface.md) tier 1, beside `guard set`), because a bound the agent can raise is not a bound. §5 widens environment scoping past the `app.` prefix: `EnvScopable` infers scopability from a **string prefix**, which is why `dns.*` and `addon.*` are cluster-wide only — it becomes a property a code DECLARES, which also unblocks the limitation ADR-0065 §3 had to record. §6's first occupants are the replica ceiling (environment-scoped), build-Job retention, add-on metric retention and **status thresholds** (cluster-scoped), each a constant today in whatever file needed it. Status thresholds are the clearest case for the cluster tier rather than the environment one: how long a pod may sit unschedulable before something is wrong is a property of the cluster's scheduler and whether it has an autoscaler, not of whether the workload is staging or production — and ADR-0074's ledger is a second consumer of the same threshold, so a status surface silent for thirty seconds while the ledger records a row at twenty would hold two different definitions of "failure" rather than merely disagreeing on tuning. Rejects a numeric field on the policy table (preserves the conflation), a constant with no guardrail (leaves other limits permanently invisible), one flat cluster-wide scope, a `burrow config` group colliding with `app config`, and a ConfigMap (a second source of truth for operator intent with no versioning story) | Proposed |
| [0069](0069-audit-covers-mutation-not-only-guardrails.md) | The audit log records mutation, not only guarded operations — `recordDecision(ctx, op, target, args, code GuardrailCode, guardErr)` says it in its signature: an operation is audited **because it passed through a guardrail**, so anything without a code is unaudited by construction. The audited set is app lifecycle (`app.deploy`, `rollback`, `delete`, `expose_public`, `autoscale`, `addon.install/remove/detach/restore`); the unaudited set is `SetGuardrail`, `SetSecret`/`UnsetSecret`, `SetConfig`, `AddEnvironment`, provider registration, `install` and `upgrade` — **the privileged set**, the operations a human runs with the admin kubeconfig that change what everything else may do. Changing an app's replica count leaves a permanent record; changing the policy deciding whether an agent may delete apps leaves none. It also undercuts [ADR-0065](0065-what-belongs-on-the-agent-surface.md), which makes two capabilities safe by denying them BY DEFAULT and states the tier holds only because `guard set` is operator-only — relax those denials and nothing records it — and [ADR-0068](0068-operational-limits-are-configuration.md) puts limits behind the same kind of setting. §1 decouples the concerns: **an operation is audited because it mutates**, with the guardrail code and disposition remaining *columns* populated when one applied rather than the reason a row exists. §3 keeps secret VALUES off the log ([ADR-0029](0029-secrets-through-the-control-plane.md)) — stated rather than assumed, because §2 adds the first audited operations whose arguments are secret-adjacent and an `args` blob is where a value gets logged by accident. §4 records the caller (human versus agent); §5 audits failures, since a log of successes hides the interesting half. §6 extends to **reads that DISCLOSE** rather than all reads, because reads outnumber mutations by orders of magnitude and a log where almost every row is a listing buries the signal: `logs` (an app prints whatever it prints, so it is the one read that can return a credential), log/metric queries, secret KEY listings, and reads of the audit log itself are recorded; `apps`, `status`, `history`, `reachability` and `guard` are not — and a read row carries target and caller, **never the disclosed content**, since embedding log lines would put in the audit log precisely the secret the read exposed. Names the awkward parts: `install`/`upgrade` run outside the Engine and may need to write to the store directly; forgetting to audit a new mutating operation is silent, which is the failure that produced this record, so a test asserting mutating Engine methods also audit would make the rule a check; retention becomes urgent WITH this record rather than eventually; and the log lives in a database an admin can edit, so it is append-only in intent rather than guarantee. Rejects auditing only `guard set`, giving the missing operations guardrail codes purely to make them auditable (the same conflation ADR-0068 removes), logging secret values, and auditing from the API layer (which knows the request but not the outcome) | Accepted |
| [0070](0070-implementation-status-lives-in-issues.md) | Implementation status lives in issues, created on acceptance — ADRs record decisions and never whether code exists ([ADR-0009](0009-honest-status.md)), which left *is this built?* answered in **three hand-maintained prose documents**: `ROADMAP.md`'s "Decided, not yet built", `CAPABILITIES.md`'s table, and `PLAN.md`'s sequencing. They drift independently, and every drift found in the 2026-07-27 sweep was prose left behind by a change rather than a disagreement about code — the README's shipped list naming a Proposed decision refused at runtime, `CAPABILITIES.md` calling an accepted ADR Proposed, both docs describing a released version as unreleased. The failure is **silent**: a stale sentence looks exactly like a current one, and the reader most likely to be misled is the one least able to tell. **An issue closes itself when a PR says `Closes #N`; a sentence does not — that is the whole argument.** §2 requires **one issue per implementable unit** rather than per ADR, which is what makes partial implementation representable (ADR-0064 shipped four sections of six, ADR-0065 one change of three; under one-issue-per-ADR each is either open or closed and neither is true). §3 has the issue name the ADR and **never the reverse**, since an accepted record cannot be edited and one accruing pointers to its own implementation is tracking status again. §4 opens issues only **after** acceptance: drafts change materially in review, and an open issue creates momentum toward accepting a record rather than examining it. §5 uses GitHub `blocked_by` for sequencing instead of prose. §6 **trims rather than deletes** — a repository should describe itself to someone reading it offline, so themes and sequencing stay in-repo and per-unit granularity moves out. §7: a bug does not wait for an ADR. Names the costs: some self-description moves to a forge a clone does not carry; issues still go stale when an ADR is superseded; acceptance gains a step whose omission leaves work invisible; and the `adr` label becomes load-bearing. Rejects more discipline with the prose trackers (discipline is what failed, in three places at once), a single prose tracker (still cannot close itself), a richer Status line in the ADR (makes accepted records mutable), a skipped test naming the ADR (most decisions are not one assertion), and deleting the prose entirely | Accepted |
| [0071](0071-a-deny-gates-the-operator-too.md) | A `deny` disposition gates the operator too, and deletion becomes a two-step act — **corrects a factual claim in [ADR-0065](0065-what-belongs-on-the-agent-surface.md) §3**, which said the denied verbs "remain fully available to the human operator CLI, which these dispositions do not gate." That is false: guardrails are evaluated in `Engine.DeleteApp`/`Engine.RemoveDomain`, **server-side**, and both CLIs reach them through the same API — `--confirm` satisfies a *hold*, not a *denial* — so `burrow app delete web --confirm` is refused. §1 confirms the resulting behaviour as intended rather than accidental: deleting an app is `guard set --env prod app.delete confirm` then delete, two deliberate steps, which is proportionate for an operation that destroys release history irreversibly and leaves the operator with a policy reflecting what they want rather than a one-off override. §2 declines the alternative that would have made the sentence true — a **caller-aware bypass** puts a new trust dimension inside the guardrail evaluator, the control plane deciding per request whether the caller is human and being right, which is an authentication question wearing a policy question's clothes and whose failure mode is a denial that does not deny; [ADR-0006](0006-guardrails-in-the-control-plane.md) keeps guardrails deterministic. The separation that already exists is the correct one: **the verb is gated for everyone, the lever is gated by structure**, since `guard set` is absent from the agent binary. §3 puts the correction in a new record because ADR-0065 is Accepted and a wrong claim is neither a typo nor a dead link — the distinction between repairing a link and rewriting a claim is what makes immutability mean anything. Consequences note that an operator's cheapest keystroke is still the cluster-wide relaxation; that ADR-0065 now contains a sentence known to be false, which puts more weight on the index row and `CAPABILITIES.md`; that `dns.delete` is worse affected since `EnvScopable` keys on the `app.` prefix so its relaxation is necessarily cluster-wide until [ADR-0068](0068-operational-limits-are-configuration.md) §5 lands; and that **per-principal roles** are the eventual principled answer and deliberately not this — burrowd deciding what a named principal may call, groundwork already laid by [ADR-0038](0038-scoped-agent-credential.md)'s principal seam, a larger surface than this product needs today. Rejects the caller-aware bypass, reverting `app.delete` to `confirm` (which depends on the agent cooperating with a hold — the property ADR-0065 declined to rely on for an irreversible operation), amending ADR-0065 in place, and letting `CAPABILITIES.md` carry the correction alone | Accepted |
| [0072](0072-release-command-for-unattended-deploys.md) | A release command for deploys nobody is driving — `burrow run` ([ADR-0048](0048-one-off-command-runner.md)) is the right primitive for migrations and **assumes a caller**, but auto-deploy ([ADR-0052](0052-pull-based-passive-deploy.md)) and push-deploys ship an image with nobody present, so a user who enables auto-deploy and changes their schema has **no supported way to migrate** — their options are an entrypoint migration that races itself across replicas and re-runs on every restart, a hand-written Job needing `kubectl` and the app's Secret (routing around the control plane and the scoped credential that makes an agent safe to point at production), or turning the feature off. This adds a **command configured once on the app** — configuration, not a per-deploy argument, because the premise is that there is no caller to supply one — run as a Job from the **new** image **before traffic moves**, and **a failure aborts the deploy**, so a migration failure becomes a deploy that did not happen rather than one that half-happened. §4 gives **rollback a separate, optional command defaulting to nothing**, because whether a rollback should touch the schema is a property of the application: forward-only teams would be harmed by anything running, teams with reversible pairs may want the schema stepped back, and a user who has not thought about it is likelier to be forward-only. **The rollback command runs from the image being rolled back FROM, not TO** — rolling back from B to A, the code that knows how to undo B's migration is in B, and running A's tool would step back one of *A's* migrations instead, which is worse than doing nothing. §5 runs it on **every** deploy path including explicit ones, since a hook that fires only sometimes defeats the point; §6 serializes per app and environment so two pushes cannot race two migrations at one database; §7 keeps Burrow ignorant of migrations — it runs a command, and versioning and idempotency are the user's tool's job. Names its limits honestly: **expand/contract is still not expressible unattended** (one hook per deploy cannot do run → deploy → run), a rollback with no command set leaves the schema ahead of the code, a rollback command that is set and wrong runs a schema change during an incident from an abandoned image, and a release command that starts failing makes an app undeployable. Rejects doing nothing and telling users to call `burrow run` ("be there" is not an answer on a path that exists so nobody has to be), replacing `burrow run` with a hook (ADR-0048's rejection stands — expand/contract needs sequencing), an init container (per-pod, so replicas race — the entrypoint problem with more YAML), running the migration **after** the deploy (the window between new code and new schema is a live outage), Burrow detecting migration tools, and **one command for both directions** (they run from different images and must default oppositely, so collapsing them makes the safe default inexpressible) | Proposed |
| [0073](0073-placement-policy-reaches-every-authored-pod.md) | Placement policy reaches every pod the engine authors, split by whose image runs — [ADR-0061](0061-deploy-pod-mutator-seam.md)'s argument was never about Deployments but about **the gap between a fixed pod literal and a real cluster's admission and scheduling constraints**, yet the seam was built for one path. The engine authors **six**: app Deployment and one-off run Job (`WithPodMutator`), build Job (`WithBuildPodMutator`, ADR-0053 §6), and **four with nothing at all** — backup **and** restore Jobs, the Postgres add-on, the metrics collector — plus a log-collector DaemonSet carrying a hard-coded blanket toleration. So **on the exact tainted-pool cluster ADR-0061 was written for**, an operator who followed it has working deploys and a backup that never runs, and **each of the four fails quietly**: a Pending Job leaves `Failed` and `Succeeded` both zero, so a waiter burns its full timeout and reports a *timeout* rather than an unschedulable pod, and a Pending add-on reads as a slow start. **The restore is the worst case** — it shares the backup's builder, so the discovery arrives during an incident. §1 makes the rule general and standing: a new authored pod path arrives with a hook or arrives undeployable. §2 splits the reach **by whose image runs** — `WithPodMutator` for the app's own image, a new `WithPlatformPodMutator` for Burrow's — because a managed operator wants the tenant's image sandboxed on tenant nodes and their own Postgres and collectors where the tenant's code is not; **one hook could serve that only by keying off an image or label to reconstruct a classification the engine already has**, and a wrong branch puts tenant code on the platform pool. §3 keeps the DaemonSet's blanket toleration as the one place a hard-coded placement field is a decision rather than an omission — a collector that skips tainted nodes silently loses those nodes' logs — while still applying the hook. §5 states what the seam is **not**: **wiring nothing sandboxes nothing**, so the fix that prompted this must not be read as "the engine now sandboxes runs" — it makes the operator's sandboxing *reach* them, and enforcement belongs to admission policy, not to a hook the same binary can decline to wire. Names the costs: two seams the engine never uses, wiring one of two is a silent partial, and the platform hook reaches **stateful** workloads, so a mutator that moves the Postgres pod where its volume cannot attach breaks the add-on rather than one deploy. Rejects one hook over everything (§2), a hook per path (exposes internal decomposition as public API for a distinction the operator has no policy about), per-field configuration (ADR-0061's endless list), **a mutating admission webhook** — the one option that would make isolation genuinely *enforceable*, rejected for ADR-0061's reasons and named as the right answer for an operator who needs enforcement — hard-coding what the managed product needs, and waiting for a report (which arrives from someone who cannot restore a database) | Accepted |
| [0074](0074-burrow-observes-what-it-manages.md) | Burrow observes what it manages, and remembers when it broke — Burrow can say an app is not available; it usually cannot say **why**, and it can never say **when it started**. `WorkloadStatus` carries an `Issue` field built for exactly this — actionable prose plus a machine-usable reason — populated in **one function for one failure class**, a failed image pull, and `imagepull.go` says so in the code: *"These are the only reasons Burrow surfaces as an Issue."* Every other blocking condition — a pod that never scheduled, a `CrashLoopBackOff`, a missing config key, an OOM kill, a volume that will not attach — reports `Available: false` with an **empty** `Issue`, so the surface built to spare users `kubectl` goes quiet on most of what actually breaks; Jobs are not in the surface at all, and add-ons, ingress and certificates have none. And **there is no watch and no informer anywhere in the tree**: every status is a question asked at the moment someone asks it, so a failure with nobody present is not recorded but merely missed — an unschedulable Job reports a *timeout* (#352), and a crashloop that healed at 02:40 is by morning indistinguishable from a week of uptime, because Events expire in an hour. §1 is the design: **current state is derived and never cached** (a cache is stalest during the incident it exists to help, and would report a healthy app that is down) while **history must be observed and stored**, because nothing reconstructs it afterwards. §2 widens the Issue vocabulary to the blocking classes, keeping the criterion the code already applies — blocking and human-fixable, not self-resolving — and is **separable and sequenced first**: no new component, no schema, and it removes most reaches for `kubectl`. §3 gives burrowd an observer, **the first thing in Burrow that runs when nobody asked it to**, scoped by the registry rather than a label. §4 records a failure as a **transition, not an event stream** — one row per object and reason with first-seen, last-seen, resolved-at and a count, so a pod that restarted four hundred times is one row saying four hundred — with bounded retention, because unbounded growth in the control plane's own database is an outage. §5 is built for the fact that failures arrive in bunches: keying on object AND reason is what lets one pod be OOM-killed and unschedulable at once without either being lost, and what makes a pod flapping between two reasons legible as the one bug it is — while the harder case is the reverse, one taint and twenty unschedulable pods, so the listing groups by shared reason and orders oldest-first, since the earliest row in a cascade is usually the one worth fixing. It stops there: **Burrow shows correlation and refuses to claim causation**, because a confidently wrong root cause during an incident is worse than none — and because that refusal is a **division of labour, not a shortfall**: Burrow does not run a model and must not acquire one, so its half is to report every failure completely in a shape something else can reason over, while **the agent's half is to turn twenty rows into a cause and a fix**, which makes completeness matter more than tidiness and puts an inference engine inside burrowd explicitly out of scope — and grouping is presentation, so an agent reading the API gets the rows and correlates on its own terms. §6 diffs **intent against the cluster** — a registered app with no Deployment, a `pending` backup whose Job is gone — the one diagnosis `kubectl` structurally cannot make, since the evidence is an absence. §7 keeps it **out of the audit log**: audit records what Burrow was asked to do and decided, this records what the cluster did afterwards, and merging them would subject an append-only security record to retention pruning. §9 **observes without remediating** — restarting a crashlooper is a mutation with nobody present, and the remedies for the failures that most invite it (OOM, unschedulable) are usually wrong. Names the costs: burrowd stops being request/response, a watch over every managed pod is real API-server load, and **a gap in the ledger reads as health unless observation coverage is itself recorded**. Rejects §2 alone (cannot answer "when did this start"), Kubernetes Events (expire, are the `kubectl` reach being removed, and cannot express §6), metrics or [ADR-0026](0026-observability-query-adapters.md)'s query seam (a state question, not a rate question — and the seam queries backends the user connected about the *app's* telemetry, which may not be installed at all), inferring root cause from a dependency graph (incomplete — it knows registered add-on bindings, not that app A calls app B — so the inference would be confident and sometimes wrong, in the worst possible place), caching current state, an event stream, folding it into the audit log, auto-remediation, and a CRD-based controller (status subresources are current-state only, and it would store the record of a failure inside the failing system) | Accepted |
