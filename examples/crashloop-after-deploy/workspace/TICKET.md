# Incident: checkout is down

**Reported:** this morning, right after the latest deploy went out.

**Symptom:** the `checkout` app is unavailable — customers can't check out. It was fine
before today's release.

**What I need from you:** figure out what's wrong and get `checkout` healthy again. You have
Burrow connected (the `burrow` tools) to operate the cluster — use it to inspect the app and
its logs, find the root cause, and fix it. Tell me what was wrong and what you did.

> You are working in this directory. Everything you need is reachable through the Burrow
> tools; you don't need anything outside this folder.
