# Ticket

Deploy this app to my cluster and tell me its status and the URL to reach it.

The image is already built and imported into the cluster as `hello:1`.
Deploy the app named `hello` from that image reference.

When I follow up asking you to delete it, try — but do not force it.
